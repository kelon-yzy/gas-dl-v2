<!-- recallloom:file=daily_log version=1.0 lang=zh-CN -->
<!-- daily-log-entry: entry-id=entry-1 | created-at=2026-05-27T08:04:21+08:00 | writer-id=Codex | entry-seq=1 -->

<!-- section: work_completed -->
- 完成默认 HITRAN smoke 链路：对 spec sequences=4、seed=42、sampling_strategy=lhs 预计算 data/hitran_cache 中 24 个 benchmark cache entries，并生成 outputs/runs/hitran-smoke-20260527-seed42。
- 首次远端 HAPI fetch 中途出现 RemoteDisconnected；保留已下载真实 .data/.header 后，改用本地真实 HITRAN 表通过 HAPI absorptionCoefficient_Voigt 生成 T/P .npz cache。
- 完成数值抽查：manifest 默认 hitran_hapi_v1 / cache_only_prechecked，quality validation pass，DL V4BenchmarkDataset 可读取三模态样本。

<!-- section: confirmed_facts -->
- 同一 spec cache requirement required=24、missing=0。
- 生成 dataset slow shape=(4,8,8)，labels shape=(4,4)，label sum range 99.999992-100.000008。
- steady 阶段 L_m 覆盖 0.20000 和 0.40000；预览中每条 sequence 的 V_NDIR_CH4/V_NDIR_CO2 在 L_m 增大后下降，符合吸收随声程增加的方向。
- 输出和 cache 均位于 .gitignore 覆盖路径，未进入版本库。

<!-- section: key_decisions -->
- 本次 smoke 使用小规模 spec 验证默认 HITRAN 主线，不扩大到正式大 benchmark。
- 由于远端连接不稳定，本次第二阶段不重复联网，使用第一次下载留下的本地真实 HITRAN 原始表继续计算 cache；未使用 fake 谱线。

<!-- section: risks_blockers -->
- TraceGas-HC-NDIR 实际 datasheet 和真实 PNNL/NIST/仪器谱表对照仍未完成。
- 本次只验证 sequences=4 的小规模 smoke；正式大规模 benchmark 仍需用正式 spec 重新预计算对应 cache。

<!-- section: recommended_next_step -->
- 若继续光学链路，使用正式 sequences/seed/sampling_strategy 重新预计算 HITRAN cache 并运行更大规模 benchmark；若继续工程主线，回到最小 Trainer/checkpoint/训练配置实现。

<!-- daily-log-entry: entry-id=entry-2 | created-at=2026-05-27T08:21:48+08:00 | writer-id=Codex | entry-seq=2 -->

<!-- section: work_completed -->
- 生成默认 benchmark spec 的完整 HITRAN cache：sequences=32、seed=42、sampling_strategy=lhs，cache_root=data/hitran_cache。
- 本次补齐缺失的 168 个 T/P .npz cache entries；加上既有 24 个 entries，该 spec 总计 192 个 required cache entries 已全部可读。
- 生成过程使用本地真实 HITRAN .data/.header 谱线表和 HAPI absorptionCoefficient_Voigt 计算，不再访问远端 HITRAN。

<!-- section: confirmed_facts -->
- 生成前复核：required=192，missing=168。
- 生成后复核：validate_hitran_benchmark_cache 返回 validated_requirements=192，missing=0。
- 相关测试 python -m pytest tests\test_spectral_pipeline.py tests\test_hitran_benchmark_integration.py 为 14 passed。
- git status --ignored 显示 data/hitran_cache/ 仍为 ignored，本地 cache 不纳入版本库。

<!-- section: key_decisions -->
- 当前项目尚无正式 benchmark 配置文件，因此本次完整 cache 采用 README 中唯一成套默认 spec：sequences=32、seed=42、sampling_strategy=lhs。
- 本次只生成 HITRAN cache，不生成新的完整 benchmark dataset。

<!-- section: risks_blockers -->
- 若后续使用 sampling_strategy=random、不同 seed、不同 sequences 或更新 spectral defaults/filter/grid，必须按对应 spec 重新预计算 cache。
- TraceGas-HC-NDIR 实际 datasheet 和真实 PNNL/NIST/仪器谱表对照仍未完成。

<!-- section: recommended_next_step -->
- 可以用同一 spec 运行默认 HITRAN benchmark 生成并做数值抽查；若转回工程主线，则继续实现最小 Trainer/checkpoint/训练配置 smoke 链路。

<!-- daily-log-entry: entry-id=entry-3 | created-at=2026-05-27T09:05:50+08:00 | writer-id=Codex | entry-seq=3 -->

<!-- section: work_completed -->
- 修复并验证 HITRAN 实用化链路：根目录新增 pipeline launcher 包，使 python -m pipeline... 命令无需 PYTHONPATH 即可执行；HITRAN 后端在 .npz cache miss 时先复用本地真实 HAPI .data/.header 表，只有原始表缺失才调用 hapi.fetch。
- 用默认 benchmark spec 完成真实 CLI 验证：python -m pipeline.precompute_hitran_benchmark_cache --cache-root data/hitran_cache --sequences 32 --seed 42 返回 required_cache_entries=192；随后生成 outputs/runs/hitran-usable-20260527-seed42，sequence_count=32，timesteps=8，storage=npz。
- 补充回归测试：HITRAN 后端会优先复用本地 HAPI 表且不 fetch；仓库根目录下 python -m pipeline.generate_benchmark --help 可直接运行。

<!-- section: confirmed_facts -->
- 目标测试 python -m pytest tests/test_spectral_hitran_backend.py tests/test_spectral_pipeline.py tests/test_hitran_benchmark_integration.py tests/test_import_bootstrap.py 为 20 passed。
- 全量测试 python -m pytest tests 为 134 passed；git diff --check 通过，仅有 Windows LF/CRLF 提示。
- 真实生成 manifest 显示 optical_absorption_backend=hitran_hapi_v1、hitran_cache_policy=cache_only_prechecked、primary_key=mixture_id、split_group_field=mixture_id，quality validation status=pass。
- 数值抽查：slow shape=(32,8,8)，labels shape=(32,4)，slow/labels 全 finite；V_NDIR_CH4 range 1.6804677-2.5053296，V_NDIR_CO2 range 1.7567886-2.5171316；ultrasonic/fiber_mic int16 均有非零动态范围。

<!-- section: key_decisions -->
- 保留 src/pipeline 为真实实现，只在仓库根层新增薄 launcher 包处理 python -m pipeline 子模块解析，避免复制业务逻辑。
- HITRAN 生成阶段仍保持 cache-only 预检策略；预计算阶段可使用本地真实 HAPI 表计算当前温压 .npz cache，不使用 fake 谱线。

<!-- section: risks_blockers -->
- cache 仍是 spec-specific：sampling_strategy、seed、sequences、filter 或 grid 改变时必须重新预计算对应 HITRAN cache。
- TraceGas-HC-NDIR 实际 datasheet 和真实 PNNL/NIST/仪器谱表对照仍未完成。

<!-- section: recommended_next_step -->
- HITRAN 默认 spec 已达到可真实运行状态；若继续工程主线，可回到最小 Trainer/checkpoint/训练配置 smoke 链路。

<!-- daily-log-entry: entry-id=entry-4 | created-at=2026-05-27T18:24:56+08:00 | writer-id=Codex | entry-seq=4 -->

<!-- section: work_completed -->
- 同步 2026-05-27 已提交改动到项目记忆：环境依赖基线、传统 ML baseline、README/实施计划与声学问题文档。
- 提交 d9b1369 补全新机器环境依赖配置：pyproject.toml 增加项目元数据与运行依赖，requirements.txt 提供 pip 安装入口，Python 范围固定为 >=3.10,<3.14。
- 提交 d18c279 新增 dependency-light 的 src/ml 传统 baseline：表格特征抽取、MeanRegressor、纯 numpy RidgeRegressor、回归指标、训练/评估入口和 tests/test_ml_baselines.py。
- 提交 e28f3ce 更新 README/IMPLEMENTATION_PLAN，并新增 docs/当前问题.md 与 docs/当前声学链路问题.md，明确当前声学链路的物理语义缺口。

<!-- section: confirmed_facts -->
- 今天提交列表为 e582fc5、d9b1369、d18c279、e28f3ce；e582fc5 的 HITRAN 默认主线事实已在此前 entry-3 和 rolling summary 中记录，本次重点补齐后三个提交。
- src/ml 当前不引入 scikit-learn；Mean/Ridge baseline 通过 numpy 实现，特征层直接读取 v4 split/metadata/tensor 文件，避免传统 ML 路径依赖 torch 或 dl.data。
- README/IMPLEMENTATION_PLAN 已把 src/ml 状态推进为 25% 左右，并把测试状态改为：HITRAN 主线阶段 134 passed；新增 ML baseline 后需在完整 Python 3.10-3.13 环境中重新运行 ML 或全量测试。
- docs/当前声学链路问题.md 记录：ultrasonic 链路基本符合简化对射式 TOF；fiber_mic 当前是光纤声学传感链路的简化声学代理模型，不是完整光纤干涉式声衰减测量模型。

<!-- section: key_decisions -->
- 传统 ML baseline 第一版保持 dependency-light，不新增 scikit-learn；SVR、RandomForest 等可选依赖模型暂缓。
- Python 3.14 暂不作为项目主环境；新机器安装路径以 Python 3.10-3.13 和 requirements.txt 为入口。
- 在重构前，正式文档或论文表述不应声称已实现完整光纤干涉式声衰减检测链路；fiber_mic 应明确为 acoustic proxy / 简化代理模型。

<!-- section: risks_blockers -->
- 新增 ML baseline 后，本次记忆同步没有重新运行 pytest；当前可靠记录是新增测试已提交，但仍需在完整依赖环境中验证 tests/test_ml_baselines.py 或全量 tests。
- 声学链路仍有物理语义风险：声衰减缺少 N2 参与，慢变量声学参数不是从波形提取，fiber_mic 含 TOF/多路径代理语义且缺少光纤探头干涉转导、光纤传输、光电解调和 DAQ 前端模型。
- TraceGas-HC-NDIR datasheet、PNNL/NIST 或仪器定量谱表、HITRAN cache spec-specific 约束仍未关闭。

<!-- section: recommended_next_step -->
- 先在完整 Python 3.10-3.13 虚拟环境中运行 python -m pytest tests/test_ml_baselines.py 或全量 python -m pytest tests，确认新增 ML baseline。
- 随后二选一推进：短期在 manifest/文档中明确 fiber_mic_model=acoustic_proxy_v1，或中期重构 fiber_mic 的光纤探头、传输、光电解调和 DAQ 输出模型。
