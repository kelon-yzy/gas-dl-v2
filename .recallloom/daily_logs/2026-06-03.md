<!-- recallloom:file=daily_log version=1.0 lang=zh-CN -->
<!-- daily-log-entry: entry-id=entry-1 | created-at=2026-06-03T10:08:24+08:00 | writer-id=Codex | entry-seq=1 -->

<!-- section: work_completed -->
- 执行 docs/LONG_SEQUENCE_PROTOCOL_PROPOSAL_2026-06-02.md 的落地计划，完成 S0-S5 可运行闭环。
- S0/S1：阶段调度抽象、时间轴 short/standard/long/xlong 预设、TCN target_timesteps 感受野覆盖校验。
- S2：新增 variable_onset、fast_transient、incomplete_recovery、multi_pulse stage_profile，并接入 stage_jitter 与 manifest/waveform_spec 溯源。
- S3：非标准 profile 的慢传感器动态改为多时间常数响应，并加入恢复残留。
- S4：新增 LSTM、Transformer、PatchTST 模型；CNN/TCN 支持 mean/last/attention 聚合；DL 数据集新增可选 window slicing/resample + jitter 增强。
- S5：ML baseline 支持 phase_filter 与 early_fraction；新增 run_baseline_protocol 用于 full/per-phase/early 对照。

<!-- section: confirmed_facts -->
- 默认 standard_exposure 且 stage_jitter=0 的兼容路径保留，不改变既有 smoke 基线语义。
- 生成产物 manifest.json 与 metadata/waveform_spec.json 记录 stage_profile、stage_jitter、phase_schedule、timesteps、dt_s。
- 目标测试通过：76 passed。
- 全量测试通过：168 passed。
- git diff --check 退出码为 0；仅提示 tests/test_ml_baselines.py 下一次会按 .gitattributes 从 CRLF 规范为 LF。

<!-- section: key_decisions -->
- 不引入 Mamba/SSM 外部依赖；当前先完成计划中的可选项以外的长序列模型闭环。
- per-phase 与 early-window baseline 基于 slow_sequence_long.csv 的实际 phase_id/timestep 生成窗口，不复制第二套阶段规则。
- 训练期数据增强默认关闭，只通过显式 augment_config 启用。

<!-- section: risks_blockers -->
- 尚未实际生成 long/multi_pulse 大规模数据集并跑完整 DL vs ridge 对比报告。
- 真实硬件标定参数和外部定量谱数据仍未补齐。

<!-- section: recommended_next_step -->
- 生成 standard/long/multi_pulse 数据集并运行 ridge、CNN/TCN、LSTM/Transformer/PatchTST 的对比实验。
- 汇总 full/per-phase/early 指标，验证长序列模型相对顺序不敏感 baseline 的差距是否随 T 增大。
- 如需长期维护 RecallLoom，后续可根据 helper 状态同步 rolling_summary.md。

<!-- daily-log-entry: entry-id=entry-2 | created-at=2026-06-03T10:26:31+08:00 | writer-id=Codex | entry-seq=2 -->

<!-- section: work_completed -->
- 继续补齐长时序协议计划的交付面：为 src/ml/cli.py 增加 --protocol 模式，可输出 full/per-phase/early baseline protocol 的 JSON 或 Markdown report。
- 新增 tests/test_ml_baselines.py 覆盖 protocol CLI JSON 输出和 --report-path Markdown 写入。
- 同步 docs/ARCHITECTURE.md 与 docs/IMPLEMENTATION_PLAN.md，记录长时序阶段协议、DL 模型扩充、ML protocol report 入口和当前验证状态。

<!-- section: confirmed_facts -->
- python -m pytest tests/test_ml_baselines.py 通过：13 passed。
- python -m pytest 全量通过：170 passed。
- git diff --check 退出码为 0；仅有 Git CRLF 规范化提示。

<!-- section: key_decisions -->
- 保留 ml.cli 默认单次 baseline 行为不变，仅在显式 --protocol 时运行 full/per-phase/early 评估协议。
- protocol report 先覆盖传统 ML baseline 的单数据集报告；跨 run 汇总、绘图和完整 DL 对比仍留给正式实验运行阶段。
- 文档不改写原提案结论，只在架构和实施计划中同步当前已落地事实。

<!-- section: risks_blockers -->
- 尚未实际生成 long/multi_pulse 大规模数据集并跑完整 DL vs ridge 对比报告。
- 真实硬件标定参数、TraceGas-HC-NDIR datasheet 和外部定量谱数据仍缺失。

<!-- section: recommended_next_step -->
- 生成 standard/long/multi_pulse 数据集，使用 python -m ml.cli --protocol 先产出传统 ML baseline report。
- 再运行 CNN/TCN/LSTM/Transformer/PatchTST 对比实验，汇总 full/per-phase/early 指标。

<!-- daily-log-entry: entry-id=entry-3 | created-at=2026-06-03T10:47:41+08:00 | writer-id=Codex | entry-seq=3 -->

<!-- section: work_completed -->
- 根据当前项目状态整理文档入口：更新 README.md、docs/ARCHITECTURE.md、docs/IMPLEMENTATION_PLAN.md，并修正 docs/OPTICAL_LINK_PRINCIPLES.md 中旧测试状态的表述。

<!-- section: confirmed_facts -->
- README.md 已同步长时序协议、ML baseline protocol report、DL 模型列表、轻量 Trainer 边界和 170 passed 验证状态。
- docs/ARCHITECTURE.md 已补充 DL training 最小闭环，并把未迁移项收敛为正式训练 CLI、scheduler、early stopping、run report、跨 run 汇总和真实标定。
- docs/IMPLEMENTATION_PLAN.md 已把 src/dl/training 状态从待实现调整为轻量闭环已完成、正式训练输出待补。

<!-- section: key_decisions -->
- 本次只做文档事实同步，不改代码、不新增 mock 或 fallback。
- 原则文档中的 145 passed 保留为 2026-05-28 历史记录，当前验证状态转由 README.md 与 IMPLEMENTATION_PLAN.md 维护。

<!-- section: risks_blockers -->
- 尚未重新运行全量 pytest；本次验证聚焦文档一致性、CLI 参数核对、git diff --check 和变更分析。

<!-- section: recommended_next_step -->
- 下一步继续按 README/IMPLEMENTATION_PLAN 中的当前边界推进：补 DL 训练 CLI，再生成 standard/long/multi_pulse 数据集并运行 ML/DL 对比。
