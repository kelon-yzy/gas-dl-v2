<!-- recallloom:file=daily_log version=1.0 lang=zh-CN -->
<!-- daily-log-entry: entry-id=entry-1 | created-at=2026-06-22T17:11:33+08:00 | writer-id=unknown | entry-seq=1 -->

<!-- section: work_completed -->
使用 pytorch-patterns 技能审查 DL 代码，对照 PyTorch 最佳实践发现 4 个问题并全部修复。

F1（严重）：梯度裁剪缺失。在 trainer.py fit() 新增 grad_clip_norm 参数（默认 0.0=不裁剪），AMP 路径加 scaler.unscale_ + clip_grad_norm_，非 AMP 路径加 clip_grad_norm_。cli.py 新增 --grad-clip-norm CLI 选项和 DEFAULT_DL_CONFIG 键。新增 2 个单测。

F2（中等）：可复现性种子不全。cli.py run() 补 torch.cuda.manual_seed_all、np.random.seed、random.seed。

F3（中等）：无显式权重初始化。BaseRegressor 加 _init_weights 静态方法，所有 8 个模型 __init__ 末尾加 self.apply(self._init_weights)。新增 2 个单测。

F4（轻微）：evaluate() per-batch .item() CUDA 同步改为设备端累积。

全量 314 测试通过，n2_head_sweep dry-run 通过。node_modules 加入 .gitignore。

<!-- section: confirmed_facts -->
全量 pytests tests 314 项全部通过。test_dl_training.py 从 33 项增至 37 项（新增 4 个：grad_clip ×2 + weight_init ×2），test_dl_losses.py 18 项保持通过。grad_clip_norm 默认 0.0 保持向后兼容。权重初始化仅影响分布（kaiming_uniform_ → kaiming_normal_），不改变训练逻辑。

<!-- section: key_decisions -->
梯度裁剪用可配置参数而非硬编码值，默认 0.0 保持现有行为。权重初始化不改 Linear bias 以保护 GasHeadNormalize prior。

<!-- section: risks_blockers -->
无。n2_head_sweep 仍被本地数据集缺失阻塞。

<!-- section: recommended_next_step -->
在服务器执行 n2_head_sweep 实验，建议尝试 grad_clip_norm=1.0 对照。
