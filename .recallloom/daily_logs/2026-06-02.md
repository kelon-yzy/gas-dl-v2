<!-- recallloom:file=daily_log version=1.0 lang=zh-CN -->
<!-- daily-log-entry: entry-id=entry-1 | created-at=2026-06-02T10:30:41+08:00 | writer-id=Claude | entry-seq=1 -->

<!-- section: work_completed -->
- 审查并修订 Karpathy 代码审查报告 docs/KARPATHY_REVIEW_2026-06-02.md：逐条对照真实代码核实全部 14 项发现；撤销 3.3（原误判为功能/测试混合修改，实为纯 CRLF 行尾符、零语义改动），修正 2.5 药方（input_format/out_dim 实被 dataset 与测试消费，改为保留并暂缓而非删除），订正 4.1 计数 8→9，补两处漏报（apply_scaler 逐行重复、resolve_split_indices 重复），附录违规计数 14→13。
- 根治工作区 CRLF 污染（129 文件、14127/14127 行）：核实方向为 HEAD 已是 LF 而工作区被污染成 CRLF；新增 .gitattributes（* text=auto eol=lf + 二进制白名单），删除并重检出把全树落地为 LF，git diff 干净、零语义损失。
- 执行报告 P0 去重重构：新建 src/common 包（RegressionMetrics 数据结构、scalers、splits 单一真相源）与 src/dl/models/heads.py（CNN1D/TCN 共享 build_regression_head）；dl.data.scalers/splits 改为 re-export，ml/features.py 删除 5 个私有副本改用 common，消除 z-score epsilon 双真相源。

<!-- section: confirmed_facts -->
- 完整测试套件 145 passed（venv：numpy 2.4.6 / torch 2.12.0+cpu / pytest 9.0.3）；受影响的 ML/DL 测试子集单独验证亦全绿。
- 去重净效果：7 个已跟踪文件 −227/+32（净减约 195 行重复代码），src/ 下无残留旧私有符号；公共 API（dl.data.scalers/splits、ml.* 导出、RegressionMetrics）经 re-export 与共享数据结构保持不变。
- CRLF 修复后 git diff --quiet 退出码 0、全树忽略空白 add=0 del=0，HEAD/索引/工作区三方一致为 LF。

<!-- section: key_decisions -->
- metrics 仅共享 RegressionMetrics 数据结构与零方差 epsilon，numpy 与 torch 计算层各自保留，不改变 DL 数值行为（遵循报告的计算层分离判断）。
- CRLF 按误触污染处理（方向核实 + 零语义内容确认），用删除重检出落地 LF，并以 .gitattributes 防复发，而非仅做一次独立 commit。
- BaseRegressor 保留不删：input_format/out_dim 已被 dataset 与测试消费，其单薄是 Trainer 尚未落地（4.2）的症状而非死抽象。

<!-- section: risks_blockers -->
- 本次为代码质量与行尾符卫生重构，未触及实验契约与数据；报告 P1（ML 训练 CLI + 真实 data/wv4-smoke 基线、DL Trainer fit→eval→checkpoint）仍未实现。
- smoke 规模（32 条）下 DL/ML 泛化指标仍无参考意义，正式基线需更大规模数据集（沿用既有判断）。

<!-- section: recommended_next_step -->
- 继续报告 P1：为 train_regressor_on_dataset 增加 argparse CLI（仅需 --dataset-dir 与 --model），并在 data/wv4-smoke 上跑出 ridge/mean 基线指标。
- 之后实现 DL Trainer（fit→eval→checkpoint），打通 DL 训练验证通道。

<!-- daily-log-entry: entry-id=entry-2 | created-at=2026-06-02T14:20:12+08:00 | writer-id=Claude | entry-seq=2 -->

<!-- section: work_completed -->
- P1.1 ML CLI（`src/ml/cli.py`）：argparse CLI 封装 `train_regressor_on_dataset`，支持 --dataset-dir/--model/--modalities/--scaler-path/--json；在 data/wv4-smoke 上跑通 ridge（train R²=0.93, val R²=0.24）和 mean 基线。
- P1.2 DL Trainer（`src/dl/training/trainer.py`）：`Trainer` 类实现 fit→evaluate→predict→save_checkpoint→load_checkpoint 闭环；附带 `build_optimizer`（adam/adamw/sgd）与 `TrainHistory`/`EpochMetrics` 数据结构；通过 checkpoint roundtrip 验证。
- P2 dtype 注释：在 `ml/models.py` 的 `_as_2d_features` 和 `_as_2d_targets` 添加 float64 用途注释（闭形式 ridge 求解的数值稳定性）。
- P2 RegressorProtocol 替换：`ml/training.py` 中 `RegressorProtocol` 替换为 `MeanRegressor | RidgeRegressor` union type（`Regressor`），遵循 review 2.4 "等 5+ 个回归器时再引入 Protocol" 的判断。
- 更新 `ml/__init__.py` 和 `dl/training/__init__.py` 导出新符号。
- 全量 145 tests passed，无回归。

<!-- section: confirmed_facts -->
- ML CLI ridge 在 wv4-smoke (32 seq) 上的基线：overall train R²=0.93, val R²=0.24；H₂ 跨 split 均良好（R²>0.94），CO₂ 泛化差（负 R²）——smoke 规模导致，非代码问题。
- DL Trainer CNN1D 10 epoch 训练通过，checkpoint 保存/加载后模型参数完全一致。
- P0 重构（split/scaler/metrics/head）已确认正确生效：`ml.metrics` 从 `common.metrics` 导入 RegressionMetrics，`dl.data.scalers/splits` 为 common 的 re-export。

<!-- section: key_decisions -->
- DL Trainer 设计为最小闭环，不引入 LR scheduler/early stopping/多 GPU——等训练规模扩大后再评估。
- `RegressorProtocol` 直接替换为 Union type，不保留向后兼容性（无外部消费者，仅 ml/training.py 内使用）。
- ML CLI 保持极简：只传 --dataset-dir 和 --model，不另建训练配置文件格式；正式配置仍走 Hydra configs/train/。

<!-- section: risks_blockers -->
- smoke 规模（32 条）下 ML CLI 和 DL Trainer 的基线指标不具参考意义，正式基线需更大规模数据集。
- DL Trainer 尚无 argparse CLI 入口，只能通过 Python API 使用。

<!-- section: recommended_next_step -->
- Karpathy 审查全部 13 条发现已修复（P0/P1/P2），报告 P1.1/P1.2/P2 清零；下一步：在更大规模数据集上建立可比基线，并为 DL Trainer 添加 CLI 入口。
