<!-- recallloom:file=daily_log version=1.0 lang=zh-CN -->
<!-- daily-log-entry: entry-id=entry-1 | created-at=2026-06-29T16:50:51+08:00 | writer-id=Claude Code | entry-seq=1 -->

<!-- section: work_completed -->
**学长 RCDW-MGDA 算法独立复现完整落地（M0-M5 全里程碑）**。

1. 创建独立子工程 `rcdw_mgda`（项目根下，与 `src` 完全隔离）：自带 pyproject.toml + configs + rcdw 包 + scripts + tests + runs 目录。
2. 严格按 `docs/学长算法/RCDW_实施指南.md` 逐文件抄写并落地：
   - 数据层：`synth.py`（合成时序与滑窗） + `preprocess.py`（占位）
   - 模型层：`single_modal.py`（NDIR 与 TCD 与 US 三模态） + `feature.py`（13 维扰动感知特征） + `error_net.py`（Softplus 恒正误差预测） + `rcdw.py`（RCDWFusion + RCDW_MGDA 集成模型）
   - 训练层：`losses.py`（WeightedMSE + StageBLoss） + `metrics.py`（MAE 与 RMSE 与 MRE 与 ARE） + `stage_a.py`（单模态预训练） + `stage_b.py`（联合训练并冻结单模态）
   - 扰动与工具：`perturbation/inject.py`（5 类扰动） + `utils/degradation.py`（hard_suppress） + `utils/normalize.py`（干基与湿基归一化）
   - 脚本：`train.py` + `eval.py` + `perturb.py` + `numerical_check.py`
   - 测试：7 个 test_*.py 文件
3. 端到端跑通 smoke 流水线：新增 `configs/smoke.yaml`（epochs=20），Stage A 三模态早停（ndir@11、tcd@19、us@16），Stage B 训练 20 epoch（loss 0.046 → 0.016），eval 出指标，perturb 出 10 张 png。
4. 文档同步：新增 `docs/学长算法/RCDW_实施完成情况.md`（完整记录里程碑、smoke 指标、5 处测试修正）；`RCDW_实施指南.md` 与 `RCDW_独立复现方案.md` 顶部加完成状态链接；`CLAUDE.md` 相关文档表新增条目。
5. **附带 sidecar 修复**：发现项目 sidecar 存在 5 天前的 stale write lock（PID 35464，2026-06-24 崩溃残留）+ rolling_summary base_revision 不一致（171 vs state.json 172）+ derived 下三个未托管文件。lock 文件触发 helper 的 `pid_is_alive` 在 Windows 上对无效 PID 抛 WinError 87 被误报 damaged_sidecar。已手工删除 stale lock 与三个 derived 残留文件，同步 file-state base_workspace_revision，validate_context 现 0 errors。

<!-- section: confirmed_facts -->
**静态测试**：rcdw_mgda 子工程 35 passed，0 failed。`python -m scripts.numerical_check` ALL CHECKS PASSED（W 与 C_fused 与 numpy 手算 max abs diff = 5.96e-08 与 2.98e-08，W 列和 = 1 误差 1.01e-06）。

**主项目零回归**：未 import `src`、未修改 `src` 与 tests 与 configs 下任何文件，主线 462 tests 不受影响。

**端到端 smoke 验证（300 样本测试集）**：
- 整体 MAE = 0.0714，RMSE = 0.0845
- 按气体 MAE：O2 = 0.0913 ‖ CO2 = 0.0584 ‖ N2 = 0.0646
- N2 MRE = 9.60%（浓度高分母大），O2 与 CO2 MRE 偏高（低浓度组分分母小），符合 Dirichlet 采样分布
- 扰动实验 5 类 × 7 强度 × 2 图 = 10 张 png 全部产出，文件清单：optical_atten 与 optical_scat 与 thermal 与 ultrasonic 与 temperature 各两张

**Stage B 联合训练有效**：trainable params = 1443（仅 ErrorNet），20 epoch 内 train loss 0.046 → 0.016。

**对实施指南的 5 处偏离记录**（全部是测试 fixture 修正，未触动核心实现）：
1. `test_output_sum_one`：加 seed + 过滤 clamp 后全零样本（Linear 偶有全负输出）
2. `test_differentiable`：`requires_grad_` 放在 abs 之后（PyTorch leaf 张量规则）
3. `test_zero_error_uses_baseline`：显式传 alpha_min=0（默认 0.1 时 W 不等于 W_base）
4. `test_degradation_trigger` 与 `test_cap_value`：断言改为"退化模态权重 < 正常模态"（hard_suppress 归一化后 cap 不再是硬上限）
5. `feature.py` register_buffer：`torch.tensor(t_var)` → `t_var.clone().detach()`（消除 UserWarning）

<!-- section: key_decisions -->
**子工程定位为玩具问题独立验证**：
- 三组分 O2 与 CO2 与 N2 仅用于验证 RCDW 思想（基线锚定 + 自适应 alpha + maxShift + 退化硬抑制 + 误差监督）的工程可行性
- 与主线 hg 四组分闭包（H2 与 CH4 与 CO2 与 N2 sum=100）与 syngas 四组分加背景（H2 与 CH4 与 CO2 与 CO + N2 背景）的组分体系**不兼容**
- 若要复用 RCDW 思想到主线，需重新设计 W_base 矩阵（主线四模态：声学 + 光学 + TCS + 光纤麦克风）、适配 syngas 开放组分、与现有 manifest 与 dataset 接口对齐

**RCDW 后续工作不阻塞主线**：
- syngas Stage Ⅲ 硬件 sim-to-real 与 hg P3 噪声调优仍是主路线
- RCDW 完整训练（200 epoch）与扰动趋势验证可作为 P2 任务延后

<!-- section: risks_blockers -->
**RCDW smoke 模型未充分训练**：20 epoch 远低于 200 epoch 默认配置，扰动曲线（如 optical_atten 下 NDIR 权重应单调降）趋势尚不显著。需运行 `python -m scripts.train --config configs/default.yaml`（CPU 约 5-15 分钟）后再观察 W 权重曲线。

**真实数据接入未做**：`rcdw_mgda/rcdw/data/preprocess.py` 当前仅占位（滑动均值滤波 + 线性校准）。真实数据需补传感器零点 + 跨度校准 + 温压湿补偿 + 滤波等环节，未列入本轮范围。

**RCDW 与主线接口尚未设计**：当前结论"RCDW 是玩具问题"是中性事实，不构成主线 blocker，但若后续决定接入主线需要单独设计 ablation 计划。

**RecallLoom helper Windows 兼容性已知问题**：unlock_write_lock 在遇到 stale PID 时即便 --force 也会因 `os.kill(stale_pid, 0)` 抛 WinError 87 而失败，需手工 rm lock 文件。若后续会话再遇到类似 damaged_sidecar 报错应先查 `.recallloom.write.lock` 是否残留。

<!-- section: recommended_next_step -->
**立即可做（CPU 5-15 分钟）**：
1. 运行完整 200 epoch 训练：`cd rcdw_mgda && python -m scripts.train --config configs/default.yaml`，目标看 Stage B test MAE 进一步下降 + 扰动 W 曲线呈现预期趋势（NDIR 在 optical_atten 下单调降、TCD 在 thermal 下单调降、US 在 ultrasonic 下单调降）。

**P2 延后任务（不阻塞主线）**：
- 若主线进入论文写作阶段且需要 baseline 对比，可考虑将 RCDW 思想（基线锚定 + 误差感知融合）适配到主线多模态训练（现有 trainer 与 fusion 网络）
- 真实标定数据接入接口设计

**主线优先**：syngas Stage Ⅲ 硬件路线规划 + hg P3 噪声调优 + 论文初稿启动，均不受 RCDW 子工程影响。
