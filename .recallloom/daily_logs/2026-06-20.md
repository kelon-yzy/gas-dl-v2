<!-- recallloom:file=daily_log version=1.0 lang=zh-CN -->
<!-- daily-log-entry: entry-id=entry-1 | created-at=2026-06-20T08:52:18+08:00 | writer-id=ZCode | entry-seq=1 -->

<!-- section: work_completed -->
校正 N2 诊断文档的证据呈现错误，并补充 S1 与 S2 解耦实验的判读盲区规则。

<!-- section: confirmed_facts -->
- ridge test 的 N2 分箱列 R2（component_metrics.x_N2.r2）实测全为负：四个区间依次为 -5.95、-2.33、-1.65、-5.57，是分箱后 N2 局部分差塌缩导致 R2 分母被压垮的几何后果，不可据分箱判可学性（来源 outputs/runs/multiwindow_n2/ridge_multiwindow_all_modalities/metrics.json）。
- 此前文档与 sidecar 误把 bin.metrics.r2（bin 内四组分加权聚合 R2，0.86-0.94）当成 N2 列 R2，得出“全浓度分箱 R2 均≥0.86”的错误表述。
- N2 可学性只由全局指标确立：ridge test N2 R2=0.712，train val test extrapolation 四 split 稳定在 0.71-0.74。主结论（gas_head 闭包残差参数化是 N2 不可学主因）不依赖分箱数据，本次校正不改变任何结论。

<!-- section: key_decisions -->
- 修正 docs/N2不可学诊断与gas_head参数化分析.md 第 2 节分箱表：区分 bin 聚合 R2 与 N2 列 R2，附明确字段来源，并把“全浓度可学”改述为“全局可学、分箱 R2 因方差塌缩为负不可判读”。
- 同步更新 rolling_summary.md：current_state 增补证据校正说明，recent_pivots 加 2026-06-20 校正条目，next_step 的判读规则补盲区。
- daily log 保留 2026-06-19 历史 entry 原样，以本条 2026-06-20 entry 记录校正，不改写历史证据。

<!-- section: risks_blockers -->
- S1 与 S2 的判读规则此前是单向有效的：N2 跳到 0.5 以上可确认残差假设，但 N2 仍低时不能直接复活非线性假设。
- 代码改动尚未执行：losses.py 的 validate_loss_model_output 仍把 weighted_component_mse 锁死在 gas_head，S1 与 S2 还无法运行。

<!-- section: recommended_next_step -->
- 执行代码改动：放开 losses.py 让 weighted_component_mse 可配 raw4 与 softmax100，free 类损失继续锁 gas_head。
- 补充判读盲区规则（已写入文档与 rolling_summary）：S1 或 S2 的 N2 仍低时，须先检查 H2、CH4、CO2 是否也低于 ridge；若四组分全低，归因为 PhaseWindowTCN 特征提取能力不足而非 N2 非线性，回到 DL 主线战略问题。
- S1（raw4）与 S2（softmax100）必须都跑：raw4 不保证 sum=100，softmax100 软保证，缺一无法区分独立参数与闭包约束各自的贡献。

<!-- daily-log-entry: entry-id=entry-2 | created-at=2026-06-20T09:08:50+08:00 | writer-id=ZCode | entry-seq=2 -->

<!-- section: work_completed -->
完成 losses.py 的 head 约束解耦、创建 n2_head_sweep 实验配置并通过 dry-run 验证。

<!-- section: confirmed_facts -->
- `src/dl/training/losses.py` 的 `validate_loss_model_output` 已按监督列数拆分校验：`weighted_component_mse`（监督全 4 列）允许 phase_window_tcn 配 raw4 / softmax100 / gas_head；`free_component_mse` 与 `weighted_free_component_mse`（只监督前 3 列、靠闭包补 N2）继续只允许 gas_head。
- 新增常量 `FREE_COMPONENT_CLOSURE_LOSSES` 和 `RAW_PERCENTAGE_VALID_HEADS`，语义比单一 `GAS_HEAD_PERCENTAGE_LOSSES` 更精确。
- 测试 `tests/test_dl_losses.py` 已同步：删除旧 "weighted_component_mse + raw4 必拒" 断言，新增 `test_weighted_component_mse_allows_non_gas_head`。全 18 项单测通过，调用方测试 60 项通过。
- 创建 `configs/experiment/n2_head_sweep/n2_head_sweep.json`：含三 run（gas_varweight 对照 / S1 raw4 / S2 softmax100），均用 compile 基线（reduce-overhead、batch=16、FP16、三窗口）。`--dry-run` 验证通过。
- 本地环境无 `wv4-formal-hitran-standard-6000` 数据集（仅有 36 条 wv4-smoke），S1/S2 无法本地执行，须在服务器运行。

<!-- section: key_decisions -->
- `validate_loss_model_output` 不改总入口集合 `GAS_HEAD_PERCENTAGE_LOSSES`，只拆 phase_window_tcn 分支的内部分支逻辑。`IMPLICIT_GAS_HEAD_MODELS`（handcraft_mlp 等）不受影响。
- S1 与 S2 的实验配置与 gas_varweight 仅 output_mode 不同，其余参数完全一致，确保唯一变量是 head 机制。
- 判读规则（三分支）已写入文档与 rolling_summary：N2 跳到 0.5+ 确认残差假设 / N2 仍低且四组分全低归因特征提取不足 / 仅 N2 低才考虑非线性耦合。

<!-- section: risks_blockers -->
- 训练受阻：本地仅有 wv4-smoke（36 条样本），无法运行 PhaseWindowTCN 正式训练。需在服务器执行。
- 执行命令：`cd 正式实验v4 && PYTHONPATH=src python -m pipeline.run_experiment --config configs/experiment/n2_head_sweep/n2_head_sweep.json`

<!-- section: recommended_next_step -->
- 在服务器（有 wv4-formal-hitran-standard-6000 数据集）执行 n2_head_sweep 实验，三 run 串行约 45 分钟。
- 取 outputs/runs/n2_head_sweep/phase_window_tcn_raw4/ 与 phase_window_tcn_softmax100/ 的 metrics.json test.x_N2.r2 对照判读。
