<!-- recallloom:file=daily_log date=2026-06-25 version=1.0 lang=zh-CN -->

<!-- daily-log-entry: entry-id=entry-1 | entry-seq=1 | writer-id=Claude Code | timestamp=2026-06-25T13:50:00+08:00 -->

<!-- section: work_completed -->
**P3 数据增强实验（Full Aug）完成并分析**：运行 cnn1d_tcn_fusion_raw4_p3_full_aug（80 epochs, apply_prob=0.5, max_shift=5, amplitude_scale=[0.95,1.05], gaussian_noise_std=0.01），test R² = +0.4998（+0.0154 vs P0-B baseline +0.4844）。CO₂ 组分改进最显著（R²: +0.27 → +0.36, +34%），CH₄ 改进 +2%（+0.44 → +0.45），H₂ 改进 +1%（+0.78 → +0.79），N₂ 轻微退化（-0.01 → -0.02）。**训练过程深度分析**：80 epochs 无过拟合（val_loss < train_loss, gap=-0.07），val R²=+0.532 > test R²=+0.500 说明泛化良好。CO₂ 在 epoch 40→79 持续改进（+0.0195/epoch），从 -0.38 逆转至 +0.38，验证数据增强对 CO₂ 特征鲁棒性提升有效。Early Stopping 应在 epoch 79 触发但未生效（最后 10 epochs 无改进），LR 始终保持 0.00015 未衰减（ReduceLROnPlateau 未触发）。**生成可视化**：training_curves.png（4 子图：loss 曲线、整体 R²、组分 R²、CO₂ 专项追踪），保存至 outputs/runs/dl_p3_full_aug/。

<!-- section: confirmed_facts -->
**P3 实验结果验收状态：❌ 未达标**：test R² = +0.4998 < 目标 0.51（差距 -0.0102），改进量 +0.0154（+3.2%）< 最低标准 +0.03。验收评级：⚠️ 轻微改进（P3 部分成功）。**CO₂ 是数据增强最大受益者**：相对 P0-B 提升 +34%，是 4 个组分中唯一显著改进者（H₂/CH₄ 仅 +1~2%，N₂ -10%）。CO₂ 在训练后期（epoch 40+）持续改进，说明时序扰动增强了 CO₂ 特征学习。**训练配置缺陷确认**：(1) apply_prob=0.5 导致仅 50% 样本被增强，有效样本数 ~6000（4000×1.5）不足目标 6000~8000；(2) 增强参数可能过于保守（max_shift=5, amplitude_scale±5%, noise_std=0.01）；(3) Early Stopping patience=10 配置正确但未触发（应在 epoch 79 停止）；(4) ReduceLROnPlateau patience=8 过宽，学习率从未衰减。**H₂/CH₄ 已接近饱和**：两者改进量 <0.02，说明高性能组分在当前架构下已触及性能上限，数据增强无法突破。**N₂ 不可学本质未改变**：R² 从 -0.01 → -0.02，验证纯端到端 raw waveform 方法无法学习 N₂ 特征，可能需要显式相位窗口统计特征（Ridge 依赖）。

<!-- section: key_decisions -->
**P3 失败定性：部分成功但整体未达标**，接下来执行**消融实验 + 参数调优作为 P3 最后尝试**。**立即执行消融实验（优先级 P0）**：运行 dl_p3_ablation_time_jitter.json、dl_p3_ablation_amplitude_scale.json、dl_p3_ablation_gaussian_noise.json，确认哪个单一策略贡献 CO₂ 改进（目标：识别 CO₂ R² > 0.35 的策略）。**条件执行参数调优（基于消融结果）**：若某单一策略使 CO₂ R² > 0.35，创建 dl_p3_stronger_aug.json（apply_prob=0.8, max_shift=10, amplitude_scale_range=[0.9,1.1], gaussian_noise_std=0.02）专注该策略优化，目标 CO₂ R² ≥ 0.40、整体 R² ≥ 0.51。**P3 放弃阈值**：若消融实验 + 参数调优后整体 R² 仍 < 0.51，则**放弃纯数据增强路线，转向 P4 Multi-scale 架构** + 保留 CO₂ 受益的增强策略作为辅助。**训练配置修复建议**：early_stopping.patience 从 10 提升至 15（避免过早停止），scheduler.patience 从 8 降至 5（更早触发 LR 衰减），下次实验应用。

<!-- section: risks_blockers -->
**P3 增强强度不足风险**：当前 apply_prob=0.5 + 保守参数可能是未达标主因，但盲目增强强度可能引入过拟合或破坏原始特征分布。消融实验必须先验证单一策略有效性，再决定是否加强。**消融实验时间成本**：3 个消融实验 × 80 epochs ≈ 6~9 小时 GPU 时间，若结果不理想需立即决策是继续调优还是转向 P4。**P4 Multi-scale 可行性未知**：即使 P3 完全失败，P4 的多尺度并行架构能否在参数量限制（100K 内）下提升性能仍不确定。可能需要先验证中等容量（96 通道）基线。**DL vs Ridge 差距依然巨大**：P3 最佳结果（+0.50）距 Ridge（+0.71）仍有 0.21 差距，即使 P3+P4 全部成功，突破 0.60 门槛仍不明朗。可能需要接受 DL 在当前数据规模下的性能上限。**N₂ 组分根本无法学习**：所有实验 N₂ R² 均为负，验证了纯 raw waveform 端到端方法的局限性。若 N₂ 是关键目标组分，则必须考虑混合模型（DL embedding + 手工特征）或放弃 DL 路线。

<!-- section: recommended_next_step -->
**立即执行**：运行 3 个 P3 消融实验，优先级顺序 time_jitter → amplitude_scale → gaussian_noise（基于 CO₂ 时序特征假设，时间抖动可能贡献最大）。每个实验完成后立即分析 CO₂ 组分 R²，若某策略使 CO₂ R² > 0.35 则标记为候选。**消融分析完成后（预计 6~9 小时）**：(1) 若识别出有效策略 → 创建 dl_p3_stronger_aug.json 专注该策略 + 参数加强，运行最后一轮 P3 实验；(2) 若所有单一策略 CO₂ R² < 0.35 → P3 数据增强路线失败，立即转向 P4 Multi-scale 架构设计 + 中等容量（96 通道）基线验证；(3) 若参数调优后整体 R² < 0.51 → 更新 improvement_plan.md 标记 P3 失败，启动 P4 或考虑混合模型方案。**并行任务**：修复训练配置（early_stopping.patience=15, scheduler.patience=5），准备 P4 Multi-scale 架构草案（多分支并行 + 注意力融合，参数预算 100K）。
