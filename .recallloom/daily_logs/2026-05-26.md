<!-- recallloom:file=daily_log version=1.0 lang=zh-CN -->
<!-- daily-log-entry: entry-id=entry-1 | created-at=2026-05-26T08:29:22+08:00 | writer-id=Codex | entry-seq=1 -->

<!-- section: work_completed -->
- 完成 Phase 4.2 训练基础组件最小实现：新增 src/dl/training 包，包含 loss 注册与构造、整体和按组分回归指标。
- 新增 tests/test_dl_training.py 覆盖 loss registry、build_loss、整体 MAE/RMSE/R2、常量目标 R2、按 COMPONENT_FIELDS 的组分指标和错误形状显式失败。

<!-- section: confirmed_facts -->
- dl.training.losses 暴露 LOSS_REGISTRY 与 build_loss，支持 mse、mae、smooth_l1、huber。
- dl.training.metrics 暴露 RegressionMetrics、regression_metrics、component_regression_metrics，组分顺序使用 sim.core.schema.COMPONENT_FIELDS。
- src/dl/__init__.py 已导出训练基础入口。
- 验证结果：tests/test_dl_training.py 11 passed；tests/test_dl_models.py + tests/test_dl_data.py 40 passed；pytest -q 106 passed。

<!-- section: key_decisions -->
- 本次只建立 loss/metrics 训练前置契约，不引入 trainer、checkpoint 或实验追踪状态。
- R2 在常量目标下采用显式语义：完全匹配为 1.0，不匹配为 0.0。

<!-- section: risks_blockers -->
- 端到端 trainer、checkpoint 管理、训练配置和实验追踪仍未实现。
- metrics 当前返回 Python float，适合 epoch 汇总；若后续需要分布式训练聚合，需要在 trainer 层另行设计。

<!-- section: recommended_next_step -->
继续 Phase 4.2：实现最小 Trainer 与 train/eval epoch 循环，接入 V4BenchmarkDataset、build_model、build_loss、regression_metrics，并补 checkpoint 保存与最小 smoke 测试。

<!-- daily-log-entry: entry-id=entry-2 | created-at=2026-05-26T08:48:17+08:00 | writer-id=Codex | entry-seq=2 -->

<!-- section: work_completed -->
- 继续推进 hitran_hapi_v1：默认 HITRAN grid 从旧窗口扩大为 CH4 2880-3180 cm-1、CO2 2250-2445 cm-1，覆盖当前行业参考滤光片 center +/- FWHM。
- 同步更新 configs/data/spectral-defaults.json、src/sim/generation/spectral/defaults.py、README 和光谱相关文档，明确旧窗口仅为早期真实下载验证，当前默认窗口需要重新预计算。
- 新增 tests/test_spectral_pipeline.py 的默认 grid 不变量，固定 config 与代码默认值一致，并要求默认 HITRAN grid 覆盖滤光片主响应。
- 已用本地真实 HAPI 1.3.0.0 在新默认窗口重新运行 precompute_hitran_spectra，生成本地 data/hitran_cache 新窗口缓存。

<!-- section: confirmed_facts -->
- 新默认 grid：CH4 2880-3180 cm-1，CO2 2250-2445 cm-1，step 0.1 cm-1，T=296 K，P=1 atm。
- 真实预计算输出：CH4 通道 absorbance_observed 约 0.8600529307，主要来自 CH4 0.8589351637；CO2 通道 absorbance_observed 约 1.4407106222，主要来自 CO2 1.4306321959。
- compare_optical_backends 使用新缓存跑通：hitran_hapi_v1 CH4 observed 约 0.858935676，CO2 observed 约 1.440706776；相对 empirical_v1 差值分别约 0.3237232445 和 1.0432326377。
- 验证结果：光谱目标测试 16 passed；全量 pytest 107 passed；git diff --check 无 whitespace error，仅有 Windows LF/CRLF 提示。
- data/hitran_cache*/ 仍是本地运行缓存并已由 .gitignore 排除，不纳入版本库。

<!-- section: key_decisions -->
- 本次不把 benchmark 主线切换到 hitran_hapi_v1，只推进 HITRAN 物理支撑路径与默认窗口一致性。
- 默认 grid 必须至少覆盖当前滤光片 center +/- FWHM，作为测试不变量固定。
- 旧 2960-3100 / 2280-2410 窗口保留为早期真实下载验证记录，不再作为当前默认窗口。

<!-- section: risks_blockers -->
- TraceGas-HC-NDIR 实际 datasheet 仍未获取，当前滤光片仍是 InfraTec 行业参考占位。
- 新窗口真实预计算已跑通，但尚未用 PNNL/NIST 或仪器数据做外部数值 sanity check。
- benchmark 主线仍使用 empirical_v1，hitran_hapi_v1 仍作为显式预计算和对照入口。

<!-- section: recommended_next_step -->
继续 hitran_hapi_v1 可选两条：优先导入 PNNL/NIST 定量谱表做外部 sanity check；或实现 benchmark 生成时显式选择 optical_absorption_backend 的配置入口，但正式切换前仍需 TraceGas-HC-NDIR datasheet。

<!-- daily-log-entry: entry-id=entry-3 | created-at=2026-05-26T09:21:06+08:00 | writer-id=Codex | entry-seq=3 -->

<!-- section: work_completed -->
- 完成 PNNL/NIST 外部定量谱表 sanity check 的通用 CSV 入口：新增 quantitative_table.py，支持显式单位 per_percent_m、per_fraction_m、per_ppm_m，并统一换算为每 1% 浓度每米吸收系数。
- 新增 pipeline.sanity_check_tabulated_spectra，可把外部谱表重采样到当前 HITRAN grid，并在同一 NDIR filter 下对比 tabulated 与 hitran_hapi_v1 的吸光度、目标气体主导性、非负贡献和数量级比例。
- 修复 sanity_check_tabulated_spectra 的全零浓度边界：入口处先校验至少一个正浓度气体，避免落入下游空谱表错误。
- 新增 tests/test_quantitative_table.py 与 tests/test_spectral_sanity_check.py 覆盖谱表加载、单位换算、禁止外推、缺谱表失败、全零浓度失败和 H2O 零浓度不触发 HITRAN 下载。

<!-- section: confirmed_facts -->
- 外部谱表 contract 为 CSV：默认列 wavenumber_cm1 与 absorption_coeff；真实 PNNL/NIST 或仪器格式后续应先转换到该 contract，不在主代码里做隐式猜测。
- resample_spectrum_to_grid 只允许目标 grid 落在外部谱表覆盖范围内，不做 extrapolation。
- sanity check 是诊断入口，不改变正式 benchmark 主线；当前 benchmark 仍使用 empirical_v1，hitran_hapi_v1 仍作为显式预计算和对照 backend。
- 验证结果：tests/test_spectral_sanity_check.py + tests/test_quantitative_table.py 为 12 passed；光学链路相关回归 16 passed；全量 pytest -q 为 119 passed；git diff --check 无 whitespace error，仅有 Windows LF/CRLF 提示。

<!-- section: key_decisions -->
- 不把真实 PNNL/NIST 数据 vendor 到仓库；先固定通用外部定量谱表 contract。
- 单位必须显式传入，禁止自动猜单位或静默默认。
- 只对正浓度气体构建外部谱表和 HITRAN gas_specs，零浓度气体不会触发无意义下载。

<!-- section: risks_blockers -->
- 真实 PNNL/NIST 数据尚未导入，当前实现完成的是通用 CSV sanity-check 路径和合成/伪 HAPI 测试。
- TraceGas-HC-NDIR 实际 datasheet 仍未获取，当前滤光片仍是 InfraTec 行业参考占位。
- benchmark 主线仍未支持正式配置切换 optical_absorption_backend；正式切换前仍需 datasheet 与外部谱表/仪器数据校验。

<!-- section: recommended_next_step -->
如果继续光学链路，下一步优先用真实 PNNL/NIST 或仪器导出的谱表转换为通用 CSV contract 后运行 sanity_check_tabulated_spectra；若暂无真实谱表，则转向实现 benchmark 生成时显式选择 optical_absorption_backend 的配置入口。

<!-- daily-log-entry: entry-id=entry-4 | created-at=2026-05-26T10:13:45+08:00 | writer-id=Codex | entry-seq=4 -->

<!-- section: work_completed -->
- 完成 hitran_hapi_v1 对 benchmark 主线的默认接入：BenchmarkGenerationSpec 默认 optical_absorption_backend 改为 hitran_hapi_v1，并保留 empirical_v1 作为显式兼容/对照路径。
- 新增 sim.generation.optical_backend 与 gas_state，统一 T/P cache key 粒度、RH 到 H2O mole pct 换算、HITRAN cache requirement 收集、cache-only 预检查和 manifest metadata。
- slow.py 的 HITRAN 路径改为逐 timestep 使用当前 blend 组分与当前 L_m 计算 NDIR equilibrium；动态滞后、漂移、随机游走和噪声仍沿用现有 slow channel 机制。
- hitran_backend.py 增加 allow_fetch=False 的 cache-only 模式和 MissingHitranCacheError；benchmark 生成前缺 cache 会在写出 dataset 前失败。
- 新增 pipeline.precompute_hitran_benchmark_cache，按 sequence_count、seed、sampling_strategy 派生同一批 conditions 并预计算所需 CH4/CO2/H2O、ch4/co2 通道、逐 condition T/P cache。
- 更新 generate_benchmark CLI，新增 --optical-absorption-backend 与 --hitran-cache-root；manifest 和 waveform_spec 写入 hitran_cache_policy、hitran_temperature_pressure_mode、h2o_policy、optical_crosstalk_policy 等字段。
- 更新 README、ARCHITECTURE、OPTICAL_LINK_PRINCIPLES、SPECTRAL_INTEGRATION_PLAN、IMPLEMENTATION_PLAN，明确默认 HITRAN、cache-only 预计算流程和 empirical_v1 显式兼容路径。

<!-- section: confirmed_facts -->
- benchmark 默认 backend 现在是 hitran_hapi_v1；旧 smoke/dl/model/lhs 测试需要旧行为时显式指定 empirical_v1。
- HITRAN benchmark 生成阶段只读 cache，不导入 HAPI、不联网、不写谱线缓存。
- HITRAN T/P key 粒度为 temperature_k=round(T_C+273.15,3)，pressure_atm=round(P_MPa/0.101325,6)。
- H2O 由 T/P/RH 换算，只参与光学吸收，不进入 label 组分和 100% 组分校验。
- HITRAN 多气体滤光片积分已经表达通道交叉响应，默认路径不再叠加 empirical apply_optical_crosstalk。
- 新增 tests/test_hitran_benchmark_integration.py 覆盖默认 HITRAN benchmark、missing cache 早失败、逐时步 L_m 影响和 T/P/RH helper 可复现；tests/test_spectral_pipeline.py 覆盖 benchmark cache 预计算 CLI。
- 验证结果：python -m pytest -q 为 125 passed；git diff --check 无 whitespace error，仅有 Windows LF/CRLF 提示。

<!-- section: key_decisions -->
- 默认生成主线切到 hitran_hapi_v1；empirical_v1 只作为显式 opt-in 兼容和对照。
- cache miss 不允许在 benchmark 生成过程中现场下载，必须先运行 pipeline.precompute_hitran_benchmark_cache。
- benchmark 专用预计算命令必须与生成命令使用相同的 sequences、seed 和 sampling_strategy。

<!-- section: risks_blockers -->
- TraceGas-HC-NDIR 实际 datasheet 仍未获取，当前滤光片仍是 InfraTec 行业参考占位；拿到 datasheet 后需复核 grid 并重建 benchmark HITRAN cache。
- 真实 PNNL/NIST 或仪器定量谱表仍未导入；通用 CSV sanity check 路径已完成，但外部实测/数据库对照仍待执行。
- 真实大规模 benchmark 使用默认 HITRAN 前需要预计算对应 conditions 的 cache，cache 文件是本地运行产物，不纳入 git。

<!-- section: recommended_next_step -->
若继续光学链路，先用目标 benchmark spec 运行 pipeline.precompute_hitran_benchmark_cache 生成真实 cache，再生成一个小规模默认 HITRAN smoke benchmark 做数值抽查；同时继续推进 TraceGas-HC-NDIR datasheet 与真实 PNNL/NIST 或仪器谱表对照。
