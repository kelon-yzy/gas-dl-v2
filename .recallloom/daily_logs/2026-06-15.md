<!-- recallloom:file=daily_log version=1.0 lang=zh-CN -->
<!-- daily-log-entry: entry-id=entry-1 | created-at=2026-06-15T14:32:23+08:00 | writer-id=unknown | entry-seq=1 -->

<!-- section: work_completed -->
- 完成 `phase_window_tcn_improvement` 服务器实验结果整理与归档。
- 归档目录为 `outputs/archive/phase_window_tcn_improvement_20260615`，包含实验配置、summary、pipeline 报告、两个 run 的完整产物，以及人工结果分析。
- 本次实验完成了 RecallLoom 既定的两个最小对照：`phase_window_tcn_gas_4mse` 和 `phase_window_tcn_gas_free`。

<!-- section: confirmed_facts -->
- `phase_window_tcn_gas_4mse` 在 test split 上 `overall R2=0.4798`、`x_N2 R2=-0.0066`、`sum_abs_error` 约为 `2e-6`。
- `phase_window_tcn_gas_free` 在 test split 上 `overall R2=0.5145`、`RMSE=6.1537`，优于 `phase_window_tcn_gas_4mse`。
- `phase_window_tcn_gas_free` 的 `test x_N2 R2=-0.0155`、`extrapolation x_N2 R2=-0.0396`，仍未转正。
- 相比 `phase_window_tcn_full_exp_rec_raw4`，`gas_head` 已将 `sum_abs_error` 从 `11.1797` 降到约 `2e-6`，说明闭包问题已显著修复。

<!-- section: key_decisions -->
- `gas_head` 作为 PhaseWindowTCN 输出头方向成立，应保留。
- `free_component_mse` 不能再被视为足以单独解决 `N2` 的主改进路线。
- PhaseWindowTCN 仍不能进入正式主线；正式 ML 主线继续保持 `ridge_multiwindow_all_modalities`。
- 若继续推进 PhaseWindowTCN，下一轮应优先做 `share_window_encoder=false` 和更深 TCN 的结构消融，而不是继续只调 loss。

<!-- section: risks_blockers -->
- 当前 PhaseWindowTCN 的主要瓶颈更像是窗口表征和相位融合能力不足，而不是单纯输出空间问题。
- `share_window_encoder=true` 可能仍在稀释相位差异，这个假设还未完成验证。
- `free_component_mse` 提升 overall 但没有改善 `N2`，继续只围绕 loss 小调参的收益可能有限。

<!-- section: recommended_next_step -->
- 保持 `ridge_multiwindow_all_modalities` 作为正式 ML phase-aware 主线。
- 将 `phase_window_tcn_improvement` 作为 DL 负结果证据写入后续实验说明和报告引用。
- 如果继续做 PhaseWindowTCN，优先启动 `share_window_encoder=false` 和更深 TCN 感受野的结构消融。
