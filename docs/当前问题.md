# 当前问题跟踪

更新日期：2026-05-28。

| 问题 | 当前状态 | 后续处理 |
| --- | --- | --- |
| 声学衰减模型未显式表达 N2 对分子弛豫吸收的贡献 | 已完成工程修正。`hidden_attenuation_v2(...)` 已增加 `alpha_n2_background_v2` 和 `f_relax_n2_eff`，当前模型标注为 `semi_empirical_multigas_relaxation_proxy_v2`。 | 仍需真实 QARS/AGA/GERG 或仪器数据标定，当前不是标准模型。 |
| 慢变量中的 `TOF`、`Amp`、`f_peak`、`A_fft_max` 等声学参数不是从波形参数计算 | 已完成核心 TOF 修正。超声链路已显式落盘生成真值、观测 TOF、峰值位置、真实/估计声速、声衰减、TOF 质量和接收标志。 | 若后续需要 FFT 频域质量指标，可继续从 waveform 派生 `f_peak_observed`、`A_fft_max_observed`。 |
| `ultrasonic` 与 `fiber_mic` 两路波形物理语义混淆，`fiber_mic` 作为声衰减链路却包含 TOF 类信息 | 已完成结构性修正。`ultrasonic` 现在是 `tof_observed_transducer_proxy_v1`；`fiber_mic` 现在是 `fiber_interferometric_proxy_v1`，走探头声压、相位转导、线性解调和 DAQ 量化链路。 | 仍需用真实光纤探头、解调器、放大器和 DAQ 标定参数替换默认占位值。 |

详细声学链路分析见 `docs/当前声学链路问题.md`。
