# 场景隔离重构计划

> 把 hg / sg / tv3 / RCDW 四个气体检测场景重组为各自独立的自包含子工程，原单仓库结构废弃。
> 分支：`refactor/scenario-isolation`（基于 `main` 创建，2026-07-07）。
> 模式参照：`rcdw_mgda/`（已有独立 pyproject / 包名 `rcdw` / 独立 tests / 独立 CLI）。

## 1. 背景与目标

当前项目四个气体检测背景的单仓库结构中，`hg`（掺氢天然气）场景的文档、配置、脚本、pipeline 文件散落在各层级根目录，与 `sg` / `tv3` 的已隔离子目录、以及三场景共用代码混在一起，可读性差。

目标：四个场景各自成为自包含子工程，每个子工程有独立 `pyproject.toml`、独立包名、独立 `tests/`、独立 CLI，能独立 `pip install -e .` 并运行。原单仓库 `src/` `docs/` `configs/` `scripts/` `tests/` 在迁移完成后整体废弃删除。

## 2. 四个场景现状

| 场景 | 代号 | 文档 | 配置 | 代码隔离 | 当前状态 |
|---|---|---|---|---|---|
| 掺氢天然气 | hg | 散在 `docs/` 根 | 散在 `configs/experiment/` 根 | `src/sim/core/schema.py` 无前缀；`src/sim/generation/` 根目录混 hg 专用文件 | 未隔离 |
| 合成气 | sg | `docs/syngas/` | `configs/experiment/sg4/` | `syngas_schema.py` + `src/sim/generation/syngas/` 子包 | 已隔离 |
| 掘进通风 | tv3 | `docs/掘进通风/` | `configs/experiment/tv3/` | `tunnel_ventilation_schema.py` + `src/sim/generation/tunnel_ventilation/` 子包 | 已隔离 |
| 学长算法 | RCDW | `docs/学长算法/` | `rcdw_mgda/configs/` | `rcdw_mgda/` 完全独立子工程 | 已完全隔离，本次不动 |

## 3. 决策共识

| 决策项 | 选择 | 说明 |
|---|---|---|
| 原结构命运 | 废弃删除 | 单轨，场景子工程是新主线 |
| 共用代码处理 | 整文件复制，后续逐场景精简 | 先跑通，再删减该场景用不到的函数 / 分支 |
| 推进方式 | 分阶段 | 先 tv3 跑通验证模式，再推广 hg / sg |
| 验收标准 | 各子工程独立 `pip install -e .` + 独立 CLI + 自身测试通过 | 参照 `rcdw_mgda/` |
| 包名 | `tv3` | 短名，import 写 `from tv3.sim... import`；hg / sg 包名待定（候选 `hg` / `sg`） |
| 数据策略 | 各子工程 `data/` 只放场景专属数据集；`hitran_cache` 等共享光谱缓存放独立共享位置 | tv3 第一阶段用 empirical 后端，不依赖 hitran_cache |
| 原结构删除时机 | 每场景就绪后立即删该场景专属部分 | 过渡期原项目 pytest 逐步失效属正常 |
| git 策略 | 新分支 `refactor/scenario-isolation`，每阶段一 commit | 全部完成验证后合并 `main` |

## 4. 共用代码边界

以下代码三场景共用，整文件复制进各子工程，后续逐场景精简：

- `src/common/`：`composition.py` / `metrics.py` / `splits.py` / `scalers.py` / `windows.py`
- `src/sim/packaging/`：数据打包、manifest、scalers
- `src/sim/validation/`
- `src/dl/`：DL 框架（manifest 驱动自动兼容三场景）
- `src/ml/`：Ridge / Mean 共用基础设施 + ROCKET
- `src/sim/generation/spectral/`：HITRAN 光谱后端
- `src/sim/generation/` 根目录共用文件：`phases.py` / `gas_state.py` / `optical_backend.py` / `optical_crosstalk.py` 等

## 5. 最终目标结构

```
项目根/
├── tunnel_ventilation/        # tv3 子工程
│   ├── pyproject.toml         # name="tunnel-ventilation", packages=["tv3*"]
│   ├── tv3/                   # 包名
│   │   ├── sim/{core,generation,packaging,validation}/
│   │   ├── dl/
│   │   ├── ml/
│   │   ├── common/
│   │   └── pipeline/
│   ├── configs/               # 从 configs/experiment/tv3/ 迁入
│   ├── docs/                  # 从 docs/掘进通风/ 迁入
│   ├── scripts/               # tv3 专属脚本
│   ├── tests/                 # tv3 专属测试
│   └── data/                  # tv3-smoke / tv3-formal
├── hydrogen_ng/               # hg 子工程（阶段 2）
├── syngas/                    # sg 子工程（阶段 3）
├── rcdw_mgda/                 # 已存在，不动
└── shared/                    # 共享资源（hitran_cache 等）
```

## 6. 第一阶段：tv3 模式验证

### 6.1 tv3 子工程目标结构

```
tunnel_ventilation/
├── pyproject.toml
├── tv3/
│   ├── __init__.py
│   ├── sim/
│   │   ├── core/
│   │   │   ├── tunnel_ventilation_schema.py
│   │   │   └── ids.py
│   │   ├── generation/
│   │   │   ├── tunnel_ventilation/    # 场景专用子包
│   │   │   ├── spectral/              # 共用，整文件复制
│   │   │   ├── phases.py              # 共用
│   │   │   ├── gas_state.py           # 共用
│   │   │   ├── optical_backend.py     # 共用
│   │   │   └── optical_crosstalk.py   # 共用
│   │   ├── packaging/                 # 共用
│   │   └── validation/                # 共用
│   ├── dl/                            # 共用（manifest 驱动）
│   ├── ml/                            # 共用 + rocket
│   ├── common/                        # 共用
│   └── pipeline/
│       ├── generate_tunnel_ventilation_benchmark.py
│       └── run_tv3_rocket_baseline.py
├── configs/                           # tv3_baseline / tv3_tcn / ... / tv3_rocket_ridge
├── docs/                              # 原 docs/掘进通风/ 全部
├── scripts/                           # run_tv3_baseline / recompute_tv3_split / run_spxy_compare
├── tests/                             # test_tunnel_ventilation_* + test_rocket_* + test_tv3_rocket_*
└── data/                              # tv3-smoke / tv3-formal
```

### 6.2 import 前缀重写规则

整文件复制后，所有 import 全局替换：

| 原 import | 子工程 import |
|---|---|
| `from sim.` / `import sim.` | `from tv3.sim.` / `import tv3.sim.` |
| `from common.` / `import common.` | `from tv3.common.` |
| `from dl.` / `import dl.` | `from tv3.dl.` |
| `from ml.` / `import ml.` | `from tv3.ml.` |
| `from pipeline.` / `import pipeline.` | `from tv3.pipeline.` |

`rcdw_mgda/` 已验证此模式（其代码全用 `from rcdw.xxx import`）。注意 `pyproject.toml` 的 `packages.find` 要匹配 `include=["tv3*"]`。

### 6.3 阶段步骤

| 步骤 | 内容 | 验收 | 状态 |
|---|---|---|---|
| 1.1 | 建 `tunnel_ventilation/` 骨架 + `pyproject.toml` + 复制文件（场景专用 + 共用整文件） | 目录结构就位 | ⬜ |
| 1.2 | 全局 import 前缀重写（`sim.` → `tv3.sim.` 等） | `python -c "import tv3"` 无报错 | ⬜ |
| 1.3 | `pip install -e ./tunnel_ventilation` + 跑 tv3 测试 | tv3 测试全通过 | ⬜ |
| 1.4 | 独立跑 CLI（generate benchmark + rocket baseline smoke） | CLI 端到端通过 | ⬜ |
| 1.5 | 删除原结构中 tv3 专属部分 | 原项目剩余（hg/sg）仍可跑 | ⬜ |

### 6.4 tv3 需要迁移的文件清单

**场景专用（从原结构迁移）：**
- `src/sim/core/tunnel_ventilation_schema.py`
- `src/sim/generation/tunnel_ventilation/`（整个子包）
- `src/pipeline/generate_tunnel_ventilation_benchmark.py`
- `src/pipeline/run_tv3_rocket_baseline.py`
- `src/ml/rocket_features.py` / `src/ml/rocket_training.py`
- `configs/experiment/tv3/*.json`（7 个配置）
- `docs/掘进通风/`（整个目录）
- `scripts/run_tv3_baseline.py` / `scripts/recompute_tv3_split.py` / `scripts/run_spxy_compare.py`
- `tests/test_tunnel_ventilation_*.py`（4 个）+ `tests/test_rocket_features.py` + `tests/test_tv3_rocket_pipeline.py` + `tests/test_spxy_split.py`（如存在）

**共用整文件复制（后续精简）：**
- `src/common/` 全部
- `src/sim/packaging/` 全部
- `src/sim/validation/` 全部
- `src/sim/generation/spectral/` 全部
- `src/sim/generation/{phases,gas_state,optical_backend,optical_crosstalk}.py`
- `src/sim/core/ids.py`
- `src/dl/` 全部
- `src/ml/{features,models,training,metrics,evaluation_protocol,cli,__init__}.py`

## 7. 后续阶段概要

### 阶段 2：hg 子工程

`hg` 场景文件散落在根目录，迁移工作量最大。需先梳理 hg 专属文件清单（`docs/` 根的 hg 文档、`configs/experiment/` 根的 hg 配置、`scripts/` 根的 hg 脚本、`src/pipeline/generate_benchmark.py` 等 hg 专属 pipeline、`tests/` 根的 hg 测试）。包名候选 `hg`。

### 阶段 3：sg 子工程

`sg` 已有 `docs/syngas/` + `configs/experiment/sg4/` + `src/sim/generation/syngas/` 子包，隔离度仅次于 tv3。包名候选 `sg`。

### 阶段 4：根级清理

四场景子工程就绪后，删除原 `src/` `docs/` `configs/` `scripts/` `tests/`，建立 `shared/` 放共享资源（hitran_cache），合并 `main`。

## 8. 数据策略

| 数据类型 | 处理方式 |
|---|---|
| 场景专属数据集（tv3-smoke / tv3-formal / wv4-* / sg4-*） | 迁入对应子工程 `data/` |
| 共享光谱缓存（`data/hitran_cache/`，20574 文件，CH4/CO2/H2O 三场景共用） | 放 `shared/hitran_cache/`，子工程通过配置路径引用 |
| tv3 第一阶段 | 用 empirical 后端，不依赖 hitran_cache，数据策略先跳过 |

## 9. 原结构删除策略

每场景子工程就绪并验收后，立即删除原结构中该场景专属部分：

- tv3 就绪 → 删 `src/sim/generation/tunnel_ventilation/`、`src/sim/core/tunnel_ventilation_schema.py`、`configs/experiment/tv3/`、`docs/掘进通风/`、`scripts/run_tv3_*.py` + `scripts/recompute_tv3_split.py` + `scripts/run_spxy_compare.py`、`tests/test_tunnel_ventilation_*.py` + `tests/test_rocket_*` + `tests/test_tv3_rocket_*`
- hg 就绪 → 删 hg 专属文件
- sg 就绪 → 删 sg 专属文件
- 全部就绪 → 删原 `src/`（剩余共用部分）/ `docs/`（剩余）/ `configs/`（剩余）/ `scripts/`（剩余）/ `tests/`（剩余）

过渡期原项目 `python -m pytest` 会逐步失效（tv3 测试删后不再跑），属正常。剩余场景在原结构里继续开发，直到该场景子工程就绪。

## 10. 风险与验证

| 风险 | 应对 |
|---|---|
| import 重写遗漏 | 1.2 完成后 `python -c "import tv3"` + `pytest --collect-only` 双验证 |
| 共用代码复制后三场景副本分叉 | 第一阶段整文件复制不改逻辑；精简在后续 commit，每场景独立验证 |
| git 历史断裂 | 用 `git mv` 而非 cp + rm（但整文件复制场景需先 cp 再 rm 原文件，commit 信息注明来源） |
| 原项目 pytest 过渡期失效 | 接受，删除时机逐场景推进 |
| 子工程 pyproject 包发现配置错误 | 参照 `rcdw_mgda/pyproject.toml` 的 `packages.find include` |

## 11. 进度跟踪

| 阶段 | 内容 | 状态 | 完成日期 |
|---|---|---|---|
| 1.1 | tv3 骨架 + pyproject + 文件复制 | ✅ | 2026-07-07 |
| 1.2 | tv3 import 前缀重写 | ✅ | 2026-07-07 |
| 1.3 | tv3 pip install + 测试通过 | ✅ | 2026-07-07 |
| 1.4 | tv3 CLI 端到端 | ⬜ | |
| 1.5 | 删除原结构 tv3 专属部分 | ⬜ | |
| 2 | hg 子工程 | ⬜ | |
| 3 | sg 子工程 | ⬜ | |
| 4 | 根级清理 + 合并 main | ⬜ | |

## 12. 参照样本

`rcdw_mgda/` 是已落地的自包含子工程，本次重构的模板：

- `rcdw_mgda/pyproject.toml`：`name="rcdw-mgda"`，`packages.find include=["rcdw*"]`，`testpaths=["tests"]`
- `rcdw_mgda/rcdw/`：包名 `rcdw`，代码用 `from rcdw.sim.generation... import`
- `rcdw_mgda/{scripts,tests,configs}/`：独立脚本 / 测试 / 配置
