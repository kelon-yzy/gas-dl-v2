# tv3 多频弛豫谱信息效率与可信反演实验计划（MRS-EI 线）

> 状态：**MEI-0 已冻结**；**MEI-1 未通过**（`mei1_inconclusive_forward_model`，2026-07-27 审查修复重跑）。**不放行 MEI-2**；**仍不授权**正式波形生成、新基准打包或硬件试验。
> 阶段前缀：`MEI-*`。它属于新的 MRS-EI 研究线，不是 MRS-3 的恢复或改名。
> 理论基础：[稀疏多频物理谱反演的通用算法改进](../deep_research/tv3_mrs_generalizable_algorithm_ideas_20260727.md)。
> 项目事实源：[掘进通风项目记忆库](../掘进通风项目记忆库.md)。
> 适用范围：`registered_simulation_domain_only`。没有真实或高保真数据时，只能形成登记仿真域内结论。
> MEI-0 最新冻结产物：`outputs/runs/tv3_mrs_ei/mei0_registry/freezes/20260727T071921821957Z_f209e893a9e5/`；`delta_num=0.02`；证据 manifest SHA256 为 `4230c8c8695a9506d3f00870bca056d13cc938f640b293cb1ce56c8c5375d862`。旧冻结目录只读保留，不作为当前事实源。
> MEI-1 最新产物：`outputs/runs/tv3_mrs_ei/mei1_forward_envelope/freezes/20260727T081544975672Z_ea58c1e41e30/`；216 点；`not_represented`（F2–F5）与 `delta_num` 不可分辨排名（span≈0.71%）共同阻止 `supported`。旧 freeze `20260727T074419780745Z_e4b2a44d640a` 只读保留，判定已撤销。

---

## 一、为什么需要一条新计划

MRS-2 已经证明，多频声速、衰减、湿度和压力观测能把联合费舍尔秩（Fisher rank）从 1 提高到 3--5。信息维度增加了，但信息强度仍然不足：登记噪声下，最佳观测臂的窄窗 O2 最大 P90 约为 4.03 vol%。MRS-6 又确认，K=4 与 K=8 的差异小于 1%，继续增加相似频点几乎没有收益。当前误差下限主要受飞行时间（time of flight, TOF）、温度、声程、幅度标定和前向模型误差共同限制。

2026-07-25 的业务决策已把 0.4 vol% 从强制门降为参考线。最低成本候选变成 `obs-cfreq` K4、TOF 标准差 0.5 μs、温度先验 0.1 K，对应最大 / 中位 P90 约 1.51 / 1.23 vol%。这给出了现实起点，却没有回答三个问题：固定 K4 是否已经是成本约束下的较优设计；现有解析求解距离统计下界还有多远；算法给出的不确定度能否相信。

因此，新计划不从“换什么网络”出发，而是按以下因果顺序推进：

```text
前向模型可信度
      ↓
测量条件是否真的增加目标信息
      ↓
确定性求解器能否充分利用已有信息
      ↓
学习型求解与后验是否带来可验证增益
      ↓
台架、高保真或真实配对证据是否支持进一步扩展
```

只要上游门没有通过，下游模型就不启动。这样可以避免用更复杂的网络掩盖硬件或前向模型问题。

## 二、现有计划提供了什么，不能继承什么

| 依据                                                                       | 可以直接复用的事实或资产                                  | 不能据此声称的内容                                     |
| ------------------------------------------------------------------------ | --------------------------------------------- | --------------------------------------------- |
| [MRS 线](tv3_multifreq_relaxation_spectroscopy_dl_implementation_plan.md) | MRS-1 弛豫前向、MRS-2 雅可比与费舍尔审计、K4 频点、216 个窄窗点     | 不得重启 MRS-3，不得改写 `mrs2_rank_upgraded_p90_fail` |
| [MRS-6 说明书](tv3_mrs6_hardware_requirements.md)                           | K4 与 K8 差异、噪声预算、1.51 / 1.23 vol% 粗精度参考、硬件短板排序 | 0.4 vol% 不再是新实验通过门；极端全栈只是理想化下界                |
| [双向 F 线](tv3_bidirectional_ultrasound_implementation_plan.md)            | 流速解耦、延迟标定、共模 / 独立抖动情景、双向质量指标                  | F5-wide 已失败；本计划不自动恢复窄域 F5/F6                  |
| [静止空气计划](tv3_static_air_feasibility_implementation_plan.md)              | 参数来源标签、干扰参数登记、独立参数留出规则                        | `flow=0` 不是现场静止空气证据                           |
| [COMSOL 计划](tv3_comsol_multiphysics_dl_implementation_plan.md)           | 多保真、模型差异、局部声学和传感器投影的审计框架                      | G2 及以后仍暂缓；不能把 G1 冒烟结果当高保真 MRS 数据              |
| B1/B7 与 D2b 结论                                                           | 训练集标定纪律、数据划分和分布外审计、默认 `raw3` 契约               | B7 不因本计划自动被替换；旧单频结果不是新谱学臂的配对对照                |

## 三、研究问题与可证伪假设

### 3.1 研究问题

1. 在相同频点数、总能量、总时长和硬件频带下，能否找到比固定 K4 更稳健的测量设计？
2. 在观测不变时，变量投影能否比普通高斯--牛顿法更稳定地消除延迟、声程和幅度等干扰参数？
3. 如果确定性求解仍有稳定残差，有限学习的展开求解器能否改善测试集和分布外条件，而不是只拟合仿真器？
4. 当问题病态或后验多峰时，能否给出覆盖率正确、允许拒绝输出的后验分布？
5. 同步多正弦、正交模型偏差和多视图一致性分别需要哪些台架、高保真或真实数据证据才能启动？

### 3.2 假设表

| 假设  | 唯一新增机制                  | 冻结对照                 | 可证伪条件                      |
| --- | ----------------------- | -------------------- | -------------------------- |
| H-B | 稳健目标导向 c 最优设计（RG-cOED）  | 固定 K4、随机 K4、中心点局部设计  | 留出工况的最坏目标信息没有超过数值保护带       |
| H-A | 干扰参数变量投影（VarPro）与有限展开   | H1、全参数高斯--牛顿法        | 测试集和分布外条件不优于确定性基线          |
| H-D | 覆盖率校准的基于仿真推断（CC-SBI）    | 剖面似然 / 拉普拉斯后验、未校准流模型 | 覆盖率不改善，或区间缩窄只来自过度自信        |
| H-C | 同步多正弦与共模剖面似然（SIMD-MRS）  | 等能量、等时长顺序 K4         | 实测跨频共模不显著，或互调与信噪比损失抵消收益    |
| H-E | 正交模型偏差灰箱反演（OMD-GreyBox） | 无偏差模型、自由残差           | 高保真或真实留出工况没有改善，或组分灵敏度被错误压制 |
| H-F | 共享潜变量多视图反演（SMVCI）       | 单视图、直接拼接             | 错误配对或时间打乱也能得到同样增益          |

## 四、统一理论框架

### 4.1 观测模型

把第 `s` 个测量条件下的观测写成：

```text
y_s = F(theta, eta_s; d_s, psi) + delta(z_s, d_s) + epsilon_s,
epsilon_s ~ N(0, Sigma_s).
```

- `theta` 是三组分目标；
- `eta_s` 是该条件下的温度、湿度、声程、延迟和增益等干扰参数；
- `d_s` 是频率、功率、停留时间、压力和湿度等实验设计变量；
- `psi` 是不同测量共享的物理参数；
- `delta` 是前向模型没有表示的偏差；
- `Sigma_s` 是逐频独立噪声与跨频共模噪声组成的协方差。

目标参数的有效费舍尔信息由舒尔补（Schur complement）给出：

```text
I_eff
  = I_theta,theta
  - I_theta,eta (I_eta,eta + Lambda_eta)^(-1) I_eta,theta.
```

这个公式决定了本计划的阶段顺序。改变实验设计 `d` 或噪声协方差 `Sigma`，有机会改变信息下限；消去干扰参数或更换求解器，只能提高估计效率；模型偏差 `delta` 没有被控制时，雅可比矩阵本身可能就是错的。

### 4.2 三类增益必须分开报告

| 增益      | 含义                                             | 对应阶段                     |
| ------- | ---------------------------------------------- | ------------------------ |
| 信息增益    | `I_eff` 增大，克拉默--拉奥下界（Cramér--Rao bound, CRB）降低 | MEI-2、MEI-5、条件成立时的 MEI-7 |
| 估计增益    | 在同一 `I_eff` 下更接近统计下界                           | MEI-3、MEI-4、MEI-6        |
| 模型可信度增益 | 修正错误的前向梯度或扩大有效适用域                              | MEI-1、MEI-7              |

任何报告都不得把估计增益写成信息增益，也不得把仿真内部校准写成现场可信度。

## 五、全局不变量

1. 不改写 v1、F4、F5-wide、MRS-2 或 MRS-6 的历史结论和产物。
2. 不恢复或重命名 MRS-3；本线使用独立阶段 `MEI-*`、独立配置、独立数据模式和独立输出目录。
3. 0.4 vol% 只作参考标注。新实验使用相对增益、覆盖率和成本门，所有门在 MEI-0 冻结后才能运行。
4. 正式模型继续直接输出 `raw3`、`out_dim=3`；不使用 N2 闭包回填、`target_transform` 或闭包残差头。
5. `mixture_id` 不得回退或重写为 `sequence_id`。新基准不依赖 `base_condition_id`、`noise_seed_index` 或 `noise_seed`。
6. 真值声速、真值衰减、真值干扰参数和仿真内部量只用于审计，不进入部署输入。
7. 所有标定、标准化、频点选择和提议分布只由训练集或独立标定集拟合；测试集和分布外集在实验开始前冻结。
8. F 线和 COMSOL G2+ 仍处于暂缓状态。本计划只能引用其已有产物，不能把条件分支写成已获启动资格。
9. 缺失参数来源、协方差、数组、清单或哈希时直接失败，不添加隐藏默认值或静默后备路径。
10. 任一增益只出现在验证集、不进入测试集和分布外集，判为未通过。

## 六、评价门如何冻结

### 6.1 不再设任意绝对精度门

新计划保留三条参考线，但不把它们混成通过条件：

- 历史参考：0.4 vol%；
- 低成本固定 K4 参考：最大 / 中位 P90 约 1.51 / 1.23 vol%；
- 极端全栈参考：最大 P90 约 0.287 vol%。

实验是否继续，主要看相对增益能否超过数值保护带。数值保护带记为 `delta_num`：

```text
delta_num = max(
  两倍有限差分步长扰动造成的 P90 相对变化,
  两倍独立新进程复算造成的 P90 相对变化,
  2%
).
```

其中 2% 高于 MRS-6 已观察到的 K4 与 K8 小于 1% 差异，用来防止把数值波动写成算法突破。SVD 容差只影响离散秩判定，不进入使用普通逆矩阵的 CRB-P90，因此单独报告 `1e-7 / 1e-6 / 1e-5` 三档容差下的秩区间与奇异值谱，不再混入 `delta_num`。离散秩结论只有在三档容差下都成立时才能作为门；否则只能作为诊断。MEI-0 必须用实际复算结果冻结 `delta_num`；若实际数值波动更大，按更大值执行。

### 6.2 四组正式门

1. **信息门**：相同成本下，稳健设计在留出工况和模型扰动集合中的最坏 P90 改善必须超过 `delta_num`，且配对自助法 95% 置信区间下界为正。
2. **求解门**：主指标固定为测试集和分布外条件下的 O2 配对 P90。新求解器相对冻结确定性基线的改善必须超过 `delta_num`，且 95% 置信区间下界为正；平均绝对误差（MAE）、收敛失败率和最坏组误差均不得退化超过 `delta_num`。CRB 效率和迭代次数用于解释机制，不能替代主指标。若某阶段只研究数值收敛，必须在 MEI-0 单独登记为数值实验，不得据此声明组分性能增益。
3. **校准门**：50% / 80% / 90% / 95% 后验区间的经验覆盖率必须落在按样本量计算的二项分布 95% 接受区间内；同时报告区间宽度和拒绝率。
4. **机制门**：硬件或多视图机制必须先通过专属负对照，才能把收益归因于共模、多视图或模型偏差约束。

门值、样本量、随机种子和自助法次数全部写入 MEI-0 登记表。正式结果产生后不得回看数据调整。

## 七、执行阶段

### MEI-0：目标、成本和证据登记冻结

本阶段只建立契约，不运行正式算法比较。

必须冻结：

- 目标方向：O2 窄窗误差，同时完整报告 CO2、N2 与闭包监控；
- 候选频带：25--200 kHz；基准 K4 为 `{25, 63, 100, 200}` kHz；
- 成本函数：频点数、总声能、总测量时间、RH / 压力切换时间和稳态等待时间，并冻结 D0 可复算成本账本；
- 前向模型族：MRS-1 单弛豫加和基线，以及湿空气 `c_eq`、H2O 参数、耦合弛豫、衍射 / 近场、换能器响应的登记扰动；
- 噪声族：逐频独立、低秩共模、混合协方差三类，但共模情景只作敏感性分析，不能代替台架证据；
- 216 个窄窗点、留出工况、有限差分步长、SVD 容差、跨容差秩报告规则和 `delta_num`；
- 所有阶段的配置摘要和 SHA256。

产物：`model_family_registry.json`、`design_space.json`、`metric_registry.json`、`stage_status.json`。

结论状态：`mei0_registry_frozen` 或 `mei0_registry_incomplete`。后者不允许进入 MEI-1。

### MEI-1：前向可信度包络与偏差方向审计

这一阶段回答“优化的是不是一个过于理想的仿真器”。它不训练自由残差网络。

1. 在 MRS-1 基线上逐项加入已登记的物理扰动或参数包络；无法实现的项保留为 `not_represented`。结构代理与合成偏差只作诊断，**不得**用注释或代理消掉 `not_represented`。
2. 比较各模型族的 O2 目标雅可比方向、**逐点**主角度（报告最大值与预注册分位数）、费舍尔秩和固定 K4 的设计排名。主角门打在逐点统计量上，不得只比较平均雅可比。
3. 构造与目标雅可比**数学上**平行、正交和混合的合成偏差（观测空间投影后再映射到 `c_f`），检查稳健设计与求解器在最不利偏差下是否仍成立。
4. 设计排名必须按 `delta_num` 做相对等价类并列；若全设计落在等价带内，记为排名不可分辨，不得把浮点 Spearman=1 写成稳定最优设计证据。
5. 正式门使用完整窄窗网格（216 点）；stride 抽样只允许冒烟。
6. 若有可用的 COMSOL 局部声学或其他高保真子集，只作为独立留出模型族；不得自动恢复 COMSOL G2+。

通过条件：设计排名在 `delta_num` 可分辨前提下跨模型族稳定，主要瓶颈稳定，且不存在未表征项足以翻转结论。结论状态为 `mei1_forward_envelope_supported`。

若关键排名随合理模型扰动翻转，或存在无数值包络的 `not_represented` 项无法证明不翻转，或设计指标跨度不超过 `delta_num`，输出 `mei1_inconclusive_forward_model`，停止 MEI-2，先补前向物理。这里不能用更复杂的稳健优化掩盖错误的雅可比矩阵。

### MEI-2：稳健目标导向 c 最优设计

本阶段只计算前向灵敏度、有效费舍尔信息和成本，不生成波形。

| 设计臂 | 设计                      | 作用             |
| --- | ----------------------- | -------------- |
| D0  | 固定 K4 `{25,63,100,200}` | MRS-6 低成本基准    |
| D1  | 等成本随机 K4                | 判断优化是否优于随机选点   |
| D2  | 中心工况局部 c 最优 K4          | 暴露只优化中心点的脆弱性   |
| D3  | 跨工况、跨模型族稳健 c 最优 K4      | 主候选            |
| D4  | 2--3 点粗测 + 条件 K4 定向测量   | 当前 D0 时间预算下不可行，只作自适应诊断 |
| D5  | 等总能量 K8                 | 等能量但不等时，只作冗余诊断     |

目标函数为：

```text
min_d  max_xi [c^T I_eff(d, xi)^(-1) c]
       + lambda_time * Time(d)
       + lambda_energy * Energy(d).
```

其中 `xi` 同时覆盖工况和模型族。低 RH、高压力死角必须进入约束，不能只在结果图中附带展示。D4 的停止规则也必须计入控制和等待成本。

MEI-0 冻结的 D0 成本为相对声能 `0.000375238095238095 s`、墙钟时间 `0.23 s`。D4 即使把 pilot 复用于总 K4，含控制开销仍需 `5.23 s`；D5 经功率缩放可等能，但墙钟时间为 `0.47 s`。因此当前只有 D1--D3 可进入等成本信息门，D4/D5 不得据此声称信息优势；若未来改变硬件时序预算，必须先新建 MEI-0 冻结，不能在 MEI-2 现场改门。

通过条件：当前只允许 D3 在相同总成本下通过信息门，并且在未参与优化的工况与模型族中仍不退化。D4/D5 只报告诊断结果，不参与通过判定。D3 通过后冻结为 `mei2_information_design_supported`；否则输出 `mei2_fixed_k4_retained`，保留固定 K4。未来若要恢复 D4，必须先按新的硬件时序预算重冻结 MEI-0。

### MEI-3：确定性变量投影与数值效率审计

先证明问题确实具有可分离结构。非线性参数块记为 `beta`，候选条件线性块记为 `a`：

```text
y = A(beta) a + b(beta) + epsilon.
```

公共延迟、对数幅度增益和逐频标定偏移只有在推导和数值测试都证明其条件线性或仿射时，才允许进入 `a`。声程 `L` 若仍与组分非线性耦合，就保留在 `beta` 中；不得为了套用变量投影而错误线性化。

| 求解器 | 内容                     |
| --- | ---------------------- |
| S0  | H1 解析 / 非线性最小二乘基线      |
| S1  | 全参数阻尼高斯--牛顿法           |
| S2  | 确定性变量投影 + 阻尼高斯--牛顿法    |
| S3  | 变量投影 + 已知真值干扰参数，仅作上限审计 |

报告 MAE、P90、相对 CRB 效率、收敛率、迭代次数、条件数、局部极小值率和分布外干扰参数表现。S2 只有在测试集与分布外条件同时通过求解门时，才能成为后续学习型模型的冻结基线。

如果不存在可靠的条件线性参数块，则输出 `mei3_varpro_not_applicable`，保留全参数高斯--牛顿法；这不是实验失败，而是对方法适用条件的正确判断。

### MEI-4：后验基线与覆盖率审计

本阶段仍使用冻结前向模型和设计，不生成正式波形数据。比较：

1. S2 剖面似然的拉普拉斯近似；
2. 参数自助法或重要性采样后验；
3. 覆盖率校准的基于仿真后验推断（CC-SBI），仅在前两种方法表现出多峰、边界偏差或计算成本问题时启动。

CC-SBI 使用置换不变集合编码器处理可变频点，输出满足单纯形约束的三组分后验。训练时联合采样组分、干扰参数、实验设计和模型族。评价必须包含基于仿真的校准（simulation-based calibration, SBC）秩直方图、经验覆盖率、负对数似然（NLL）、连续秩概率评分（CRPS）、后验预测检验和分布外覆盖率。

若覆盖率正确但区间很宽，结论只能是“观测信息不足”。不得通过温度缩放或缩窄先验制造高精度。结论状态为 `mei4_posterior_calibrated`、`mei4_deterministic_posterior_retained` 或 `mei4_uncertainty_failed`。

### MEI-5：同步多正弦与跨频协方差台架门

前置条件是具备参考负载、重复采集能力、同步激励链路和可核对的总能量。没有硬件时，本阶段保持 `blocked_by_hardware_evidence`，不得用有利的仿真相关性代替实测。

执行顺序：

1. 在空腔或参考负载上重复采集顺序 K4，估计跨频误差协方差；
2. 用对角、低秩共模和混合协方差模型做留出重复的似然比较；
3. 只有共模方差的置信区间排除 0，且低秩模型在留出重复上优于对角模型，才生成低峰均比多正弦；
4. 在总声能、总时长和换能器工作区间相同的条件下比较顺序 K4 与同步 K4；
5. 报告目标 CRB、信噪比、互调、峰均比、克拉默斯--克勒尼希残差和设备温升。

如果共模证据不成立，输出 `mei5_independent_noise_retained`；如果共模成立且等成本目标信息通过信息门，输出 `mei5_multisine_supported`。互调或独立噪声主导时停止多正弦分支。

### MEI-6：独立谱学基准与学习型求解协议

只有 MEI-2 至少证明一种设计优于固定 K4，或者 MEI-3 明确证明确定性求解仍存在可学习的稳定误差，才允许建立本阶段。它使用新数据模式 `tunnel-ventilation-mrs-ei-1` 和新基准 `tv3-mrs-ei-*`，不复用 MRS-3 名称。

最低数据形态是稀疏谱观测，不默认生成全波形：

```text
frequency_hz, c_observed, alpha_observed(optional),
quality, design_id, view_id, device_profile_id,
T_C, P_MPa, H_RH, L_m
```

若 MEI-5 通过，再增加复传递函数和跨频协方差字段。任何真值谱只进入审计数组。

正式模型矩阵：

| 模型            | 作用                     |
| ------------- | ---------------------- |
| H1            | 解析 / 非线性最小二乘基线         |
| GN            | 全参数高斯--牛顿法             |
| VarPro        | MEI-3 冻结确定性求解器         |
| NP-VPNet      | 只学习预条件、阻尼、异方差权重或小型残差先验 |
| CC-SBI        | 只在 MEI-4 获准时进入         |
| 黑盒 MLP / ARGD | 容量匹配负对照，不承担创新声明        |

NP-VPNet 每一步都必须调用同一个前向模型和数据一致性项。若它不优于确定性 VarPro，就输出 `mei6_learned_solver_no_gain`，不增加展开层数或主干网络。

### MEI-7：高保真 / 真实数据条件分支

本阶段同时容纳正交模型偏差和共享潜变量，但二者不能互相替代。

#### 7.1 正交模型偏差

前置是独立的高保真或真实标定数据。比较无偏差模型、自由残差和正交模型偏差：

```text
Phi_perp = [I - J(J^T W J)^(-1)J^T W] Phi.
```

正交约束相对于 O2 / CO2 目标雅可比定义，防止残差网络吞掉组分灵敏度。若真实偏差主要沿目标方向，必须扩大不确定度或补充物理，不能强制投影后宣称成功。

#### 7.2 共享潜变量多视图

前置是同一 `mixture_id` 在组分稳定窗口内的多频、双湿或双压配对。组分潜变量共享，T、RH、P、延迟和增益按视图独立。必须执行错误配对、时间打乱、单视图和屏蔽设计标签四个负对照。

只有正确配对相对全部负对照，在测试集、分布外条件和后验预测检验上都改善，才能输出 `mei7_multiview_supported`。错误配对也改善时，应判为泄漏或普通正则化效应。

### MEI-8：综合判定与路线收尾

最终只允许以下结论状态：

| 结论状态                                | 含义                                 |
| ----------------------------------- | ---------------------------------- |
| `mrs_ei_information_gain_supported` | 新测量设计在登记模型族和留出工况中稳定降低目标下界          |
| `mrs_ei_estimation_gain_only`       | 信息下限未变，但求解器或后验更接近下界、校准更可信          |
| `mrs_ei_fixed_k4_retained`          | 优化设计没有越过数值保护带，保留 MRS-6 固定 K4 粗监测候选 |
| `mrs_ei_forward_model_inconclusive` | 前向模型扰动足以翻转结论，先补物理或高保真数据            |
| `mrs_ei_hardware_evidence_required` | 共模、多正弦或真实配对机制缺少硬件证据                |
| `mrs_ei_simulation_only`            | 仿真域内结果成立，但不能形成现场能力声明               |

任何状态都必须同时给出相对 0.4 vol% 参考线、相对固定 K4 粗精度参考和对应成本。不得只给单个最优数字。

## 八、数据划分、泄漏控制与统计协议

1. 以 `mixture_id` 为组分主键；同一混合物的多个视图不得跨训练集和测试集。
2. 模型族、设计条件、环境边界和设备配置分别建立分布外划分；联合极端条件单列报告。
3. 相邻设计点不能随机拆分后伪装成分布外泛化。
4. 自适应设计只使用当前样本已经观测到的信息，不能读取真值组分或未来视图。
5. 每个正式比较至少使用 3 个数据划分随机种子和 3 个训练随机种子；确定性求解器也要报告多初值稳定性。
6. 配对差值使用分层自助法，层级至少包含混合物和设计条件；同时报告效应量与 95% 置信区间。
7. 多重假设按阶段族控制，不把六个假设的最佳结果挑出来单独报告。
8. 泄漏负对照触发后，冻结该次运行，完成来源审计前不得引用任何性能指标。

## 九、产物与文件边界

### 9.1 计划输出目录

```text
outputs/runs/tv3_mrs_ei/
  mei0_registry/
    freezes/<freeze_id>/
  mei1_forward_envelope/
  mei2_robust_design/
  mei3_varpro_audit/
  mei4_posterior_calibration/
  mei5_multisine_bench/
  mei6_model_protocol/
  mei7_real_or_high_fidelity/

outputs/summary/tv3_mrs_ei/
  gate_matrix.csv
  design_comparison.csv
  solver_comparison.csv
  calibration_report.json
  final_verdict.json
```

阶段目录只追加新运行，不覆盖旧证据。汇总文件只能引用阶段产物的清单和哈希，不得手工重录指标形成第二来源真相。

### 9.2 预期代码边界

| 范围         | 建议文件                                                     | 创建条件          |
| ---------- | -------------------------------------------------------- | ------------- |
| 登记与设计      | `configs/tv3_mrs_ei/`、`tv3/audit/mrs_ei_design.py`       | MEI-0 / MEI-2 |
| 前向包络       | `tv3/audit/mrs_ei_forward_envelope.py`                   | MEI-1         |
| 变量投影       | `tv3/ml/mrs_varpro.py`                                   | MEI-3 证明适用后   |
| 后验推断       | `tv3/dl/models/mrs_sbi.py`                               | MEI-4 条件满足后   |
| 多正弦        | `tv3/sim/generation/tunnel_ventilation/mrs_multisine.py` | MEI-5 台架门通过后  |
| 新数据模式      | `tv3/sim/core/tunnel_ventilation_mrs_ei_schema.py`       | MEI-6 获准后     |
| 模型偏差 / 多视图 | 独立模块与配置                                                  | MEI-7 数据前置满足后 |

共享的 MRS-1 前向函数保持单一来源。不得在设计器、求解器和后验模型中复制三份弛豫公式。

## 十、验证清单

### 10.1 数值与单元测试

- 有效费舍尔信息的舒尔补与直接块矩阵求逆一致；
- 目标信息随新增独立观测单调不减；
- 重复同一灵敏度方向不会被误判为升秩；
- 变量投影解与联合最小二乘解在可分离测试问题上相同；
- 不可分离参数不会被错误放入线性块；
- 单纯形投影保持非负和为 100%；
- 共模 / 独立协方差的合成数据可以被正确区分；
- 缺失来源、哈希、数组或不允许的真值输入时显式失败。

### 10.2 最小执行顺序

```text
MEI-0 登记冻结
  → MEI-1 前向包络
  → MEI-2 稳健设计
  → MEI-3 确定性求解
  → MEI-4 后验审计

硬件证据具备后：MEI-5
MEI-2/3/4 至少一项通过后：MEI-6
高保真或真实配对数据具备后：MEI-7
最后统一进入 MEI-8
```

首期 MEI-0 至 MEI-3 不生成正式波形、不使用 GPU，也不需要恢复 F 线或 COMSOL G2+。

## 十一、停止条件

1. MEI-1 显示合理前向扰动会翻转设计排名：停止实验设计优化，先补物理。
2. MEI-2 没有设计越过 `delta_num`：冻结固定 K4，不继续两阶段自适应或增加频点。
3. MEI-3 变量投影不适用或不优于全参数高斯--牛顿法：保留确定性基线，不启动 NP-VPNet。
4. MEI-4 覆盖率失败：不得用该后验做拒绝策略或精度声明。
5. MEI-5 没有实测共模证据：不生成有利相关噪声，不启动多正弦模型训练。
6. MEI-6 学习型模型不优于确定性 VarPro：停止增加展开层、注意力或变换器主干。
7. MEI-7 缺少高保真 / 真实数据：OMD-GreyBox 只能做合成证伪，不能形成仿真到真实结论。
8. 错误配对或时间打乱仍带来多视图增益：判泄漏，停止该分支。
9. 任一结果只在验证集或单一模型族成立：判未通过。
10. 新提案试图改写历史结论、恢复 MRS-3、回退主键语义或绕过来源登记：直接拒绝。

## 十二、文献依据与项目映射

完整文献表和逐条核验见[理论报告](../deep_research/tv3_mrs_generalizable_algorithm_ideas_20260727.md)。本计划采用的核心依据如下：

| 理论或方法       | 依据                                                             | 在本计划中的作用               |
| ----------- | -------------------------------------------------------------- | ---------------------- |
| 含干扰参数的有效信息  | Fewster 与 Jupp [6]                                             | 区分信息增益与估计增益            |
| 变量投影        | Golub 与 Pereyra [7]；Español 与 Pasha [8]                        | MEI-3 的降维求解与适用条件       |
| 算法展开        | Monga 等 [9]                                                    | 限制 NP-VPNet 只学习少量求解器参数 |
| c 最优与目标导向设计 | Sagnol [10]；Wu 等 [11]；Alexanderian 等 [12]                      | MEI-2 的目标函数、成本和模型不确定性  |
| 多正弦与时基误差    | Sanchez 等 [13]；Schoukens 等 [14]                                | MEI-5 的等能量比较与协方差建模     |
| 基于仿真推断与校准   | Cranmer 等 [15]；Lueckmann 等 [16]；Falkiewicz 等 [17]；Talts 等 [18] | MEI-4 的覆盖率门和失败解释       |
| 正交模型偏差      | Kennedy 与 O'Hagan [19]；Plumlee [20]；Plumlee 与 Joseph [21]      | MEI-7 防止偏差项吞掉组分灵敏度     |
| 多视图可辨识性     | Gresele 等 [22]；Yao 等 [23]                                      | MEI-7 的共享潜变量与负对照       |

## 十三、评审时必须回答的问题

1. `delta_num` 的复算方式是否足以区分数值波动和真实增益？
2. MEI-1 的模型族是否覆盖了当前已知的主导缺失物理？
3. 固定 K4 的总能量和总时长如何与自适应设计严格配平？
4. 哪些参数能够严格放入变量投影的条件线性块？
5. 在没有硬件时，MEI-5 是否应保持完全阻塞？
6. 新基准的配对视图能否保证同一 `mixture_id` 在测量窗口内组分稳定？
7. 哪个阶段结果足以支持从“待评审”转为“允许生成新数据”？

在这些问题完成评审并写入 MEI-0 登记表之前，本计划不启动正式运行。

> **MEI-0 重冻结记录（2026-07-27）**：上述 7 问已写入三份 registry 的 `review_answers`。`delta_num` 经 216 点 obs-cfreq K4、FD 步长扰动和两个独立新进程复算冻结为 **0.02**；FD 最大相对变化为 `4.527697442120648e-05`，独立进程变化为 `0`，不稳定 FD 点为 `0`。SVD 容差 `0.1× / 10×` 分别使 `81 / 97` 个点的离散秩改变，故后续秩结论必须跨冻结容差网格一致。结论状态 `mei0_registry_frozen`，允许进入 MEI-1 前向包络审计；正式波形生成仍禁止。
>
> **MEI-1 审查修复与重冻结（2026-07-27）**：旧 freeze `…e4b2a44d640a` 的 `mei1_forward_envelope_supported` 已撤销。修复后正式门使用 216 点；`not_represented`（F2–F5）强制阻断；合成偏差按目标 O₂ 雅可比对齐（平行 / 正交 / 混合观测角约 `0° / 90° / 45°`）；排名按 `delta_num` 等价类并列；主角门使用逐点 `max_deg`；evidence manifest 纳入审计代码、运行脚本、共享前向与上游 MEI-0 manifest，且不再向阶段根目录写可变副本。最新 freeze `…ea58c1e41e30` 结论为 `mei1_inconclusive_forward_model`：F2–F5 未表征，且 15 个 K4 跨度约 `0.71%` 低于 `delta_num`。`allowed_next_stage=null`；不放行 MEI-2；正式波形仍禁止。
