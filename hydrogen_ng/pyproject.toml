[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "hydrogen-ng"
version = "0.1.0"
description = "hydrogen_ng scenario: H2/CH4/CO2/N2 gas detection."
requires-python = ">=3.10,<3.14"
dependencies = [
    "numpy",
    "scipy",
    "scikit-learn",
    "torch",
    "hitran-api",
]

[project.optional-dependencies]
dev = [
    "pytest",
]

[tool.setuptools.packages.find]
include = ["hg*"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
